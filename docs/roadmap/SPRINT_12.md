## Sprint 12 — Technique & i18n

> **2 taches** | Priorite : **Basse** | Origine : Vague 10, Piste C
> Objectif : preparer l'infrastructure pour la montee en charge et l'internationalisation.
> Prerequis : Sprints 1-6 recommandes (contenu stable avant scaling)

---

### 134 — Load testing & scaling (M | ★★)
> Prerequis : ∅
> Avancement : sous-phases 1 (infra k6 + scenario `guest-browsing`), 2a (scenario `metrics-stress`), 2b (scenario `mercure-streaming`), 2c (scenario `authenticated-gameplay`), 2d (recueil des goulots + plan d'optimisation priorise — `docs/LOAD_TESTING_BOTTLENECKS.md`), 3a (indexes ciblés pour les collectors `/metrics`), 3b (cache TTL des collectors `/metrics` — jalon C complet), 3c (suppression du produit cartesien dans `MobRepository::findByMapWithMonster`), 3d (partial index `idx_fight_in_progress`), 3d.doc (mise a jour `docs/LOAD_TESTING_BOTTLENECKS.md` avec etat des jalons + section "Resultats observes"), 3e (script `run-all.sh` pour lancer les 4 scenarios k6 sequentiellement) et 3f (cloture analytique du jalon D : `idx_player_map_coords` non actionable car coordonnees stringifiees `"x.y"` filtrees en PHP, jalon D officiellement termine) livrees.
- [x] Script k6/Locust pour simuler 100+ joueurs simultanes — infrastructure k6 (`scripts/load-test/`) + scenario `guest-browsing` (home, login, register, demo, /health, /metrics). Ramp-up/plateau/ramp-down configurable, thresholds p95<800ms + <1% erreurs, export JSON pour CI. Documentation dans `scripts/load-test/README.md`.
- [~] Identification goulots d'etranglement (DB, Mercure, FrankenPHP)
  - [x] **Sous-phase 2a — Scenario `metrics-stress` (DB via Prometheus)** (2026-04-24) : nouveau scenario `scripts/load-test/scenarios/metrics-stress.js` focalise sur l'endpoint `/metrics` (collecte Prometheus qui declenche plusieurs `COUNT()` Doctrine sur `player`, `fight`, `mob` via les collectors de `App\Monitoring\*`). Charge soutenue **sans think-time** (20 VUs par defaut = equivalent plusieurs centaines de VUs realistes). Thresholds dedies plus stricts que le default (p95<500ms, p99<1.5s, http_req_failed<0.5%). Metriques custom (`metrics_collect_latency`, `metrics_payload_fail`) pour decoupler la latence de collecte du reste du run. Documentation dediee dans `scripts/load-test/README.md` (section "Scenario : metrics-stress") avec avertissement "ne pas lancer en production sans coordination" et piste de diagnostic `pg_stat_statements` / pool Doctrine / collectors a cacher. Zero impact applicatif (scenario JS uniquement). Permet de detecter les regressions de requetes Doctrine, la saturation du pool PostgreSQL et l'impact du volume de Fight/Mob sur la latence /metrics. Prepare la suite : scenarios authentifies (map/combat/HdV), Mercure streaming, et plan d'optimisation (connection pooling, cache Redis).
  - [x] **Sous-phase 2b — Scenario `mercure-streaming` (SSE)** (2026-04-25) : nouveau scenario `scripts/load-test/scenarios/mercure-streaming.js` qui mesure la capacite du hub Mercure integre a FrankenPHP (Caddy) a tenir des abonnes SSE concurrents sur le topic `map/move` (le plus actif en jeu : `map_pixi_controller` maintient une `EventSource` ouverte en permanence, chaque mouvement publie). Chaque VU envoie un `GET /.well-known/mercure?topic=map/move` avec `Accept: text/event-stream` et `timeout: SUBSCRIBE_DURATION + 2s` (defaut 30s) ; la connexion est tenue ouverte cote serveur (heartbeats SSE) puis coupee par k6 au timeout, et le VU recommence. Metriques custom : `mercure_subscribe_latency` (Trend, base sur `timings.waiting` = TTFB SSE = premier heartbeat ou evenement, proxy d'etablissement de subscription), `mercure_hold_duration` (Trend, duree reelle de maintien) et `mercure_subscribe_fail` (Rate, statut inattendu / content-type non `text/event-stream` / connexion coupee precocement). Thresholds : `mercure_subscribe_latency` p95<1s + p99<3s, `mercure_subscribe_fail` <2%, `mercure_hold_duration` p95 >= `(SUBSCRIBE_DURATION-1)*1000` ms (detecte les coupures precoces de proxy / load balancer). Topic surchargable via `MERCURE_TOPIC=event/announce` pour tester d'autres flux (`map/respawn`, `map/avatar`, `event/announce`, `fight/{id}/turn`, `guild/influence/{id}`). Documentation dediee dans `scripts/load-test/README.md` (section "Scenario : mercure-streaming") avec piste de diagnostic : logs Caddy/FrankenPHP, `ulimit -n` (FD), `idle_timeout` Traefik, config Mercure (`heartbeat`, `dispatch_timeout`). Note explicite sur la limite de mesure : k6 stock ne sait pas decouper un flux SSE evenement par evenement, donc la latence publish->receive bout-en-bout necessitera une variante avec extension xk6-sse (jalon ulterieur). Zero impact applicatif (scenario JS uniquement, lecture seule sur le hub). Permet de mesurer la capacite reelle de subscriptions concurrentes (objectif Sprint 12 : 200 abonnes simultanes), de detecter les coupures precoces de proxy, et de valider la cible "200 joueurs simultanes sans degradation" sur le canal Mercure.
  - [x] **Sous-phase 2c — Scenario `authenticated-gameplay`** (2026-04-26) : nouveau scenario `scripts/load-test/scenarios/authenticated-gameplay.js` qui couvre la chaine complete login (CSRF + session) + boucle de chargement de la carte cote client. Chaque VU iteration : (1) `GET /login` puis extraction du `_csrf_token` du HTML via regex, (2) `POST /login` avec `email` + `password` + `_csrf_token` + `redirects: 0` pour distinguer succes (302) de l'echec (200 = re-render du form), (3) navigation `GET /game` (dashboard) + `GET /game/map` + `GET /api/map/config` + `GET /api/map/cells?x=0&y=0&radius={MAP_RADIUS}` + `GET /api/map/entities?radius={MAP_RADIUS}` + `GET /game/inventory`. k6 maintient automatiquement un cookie jar par VU : la session Symfony reste valide pour toutes les requetes suivantes. Credentials supportes via 2 modes : `TEST_USER_EMAIL` + `TEST_USER_PASSWORD` (compte unique, simple) ou `TEST_CREDENTIALS_FILE` pointant vers un JSON `[{email, password}, ...]` (round-robin sur les VUs, recommande au-dela de 20 VUs pour eviter la serialisation des sessions Symfony sur un meme compte). Metriques custom : `auth_login_latency` (Trend, mesure du round-trip GET form + POST submit), `auth_login_fail` (Rate, taux de logins echoues), `authed_request_fail` (Rate, requetes authentifiees qui ne retournent pas 200 + content-type attendu), `authed_map_api_latency` (Trend, latence des 3 endpoints `/api/map/*`). Thresholds dedies : `http_req_duration` p95<1500ms / p99<3s (vs 800ms/2s default car les routes /game declenchent plus de requetes Doctrine), `http_req_failed`<2%, `auth_login_fail`<5% (marge pour les CSRF expirees sous charge), `auth_login_latency` p95<2s, `authed_request_fail`<2%. Fonction `setup()` rejette le run si aucun credential n'est fourni (fail-fast plutot que silencieux). Documentation dediee dans `scripts/load-test/README.md` (section "Scenario : authenticated-gameplay") avec format JSON, run cible Sprint 12 (200 VUs / 5min / pool 50 comptes) et pistes de diagnostic (firewall Symfony, indexes /api/map/*, pool Doctrine, rendu Twig). Zero impact applicatif (scenario JS uniquement, lecture seule sur les routes GET — aucun POST de mouvement / combat dans cette premiere iteration). Permet de mesurer pour la premiere fois la tenue en charge des routes protegees `^/game` et `^/api/`, et de valider la cible "200 joueurs simultanes sans degradation" sur la boucle principale du jeu (login + carte + inventaire).
  - [x] **Sous-phase 2d — Recueil des goulots identifies + plan d'optimisation priorise** (2026-04-26) : nouveau document analytique `docs/LOAD_TESTING_BOTTLENECKS.md` (315 lignes) qui synthetise les goulots probables identifies a partir des 4 scenarios k6 livres (guest-browsing, metrics-stress, mercure-streaming, authenticated-gameplay). Le document est structure en 5 sections + annexe : (1) couverture des scenarios — table croisee scenario/cible/couche stressee ; (2) goulots probables par couche — DB (Doctrine + collectors `/metrics` non caches, pool de connexions sans pgbouncer, indexes composites manquants `Player(map_id, x, y)` / `Mob(map_id, died_at)`, N+1 latents, `updated_at >= NOW()-15min` sans index), Mercure (`ulimit -n` insuffisant, `idle_timeout` Traefik, `heartbeat`/`dispatch_timeout` non configures, throughput `map/move` non batche), application PHP (cout argon2 `LoginFormAuthenticator`, verrou de session Symfony, rendu Twig `/game/map`), cache applicatif (`MetricsCollector` filesystem -> verrouillage d'inode sous concurrence) ; (3) plan d'optimisation priorise en 6 jalons (A : Redis cache, B : PgBouncer, C : cache des collectors `/metrics` + indexes `updated_at`/`died_at`, D : indexes composites APIs map, E : hardening Mercure, F : scaling horizontal) avec pour chaque jalon objectif, taches detaillees, gain attendu et re-run k6 attendu ; (4) indicateurs de succes — table de seuils cibles par scenario a 200 VUs ; (5) prochaines etapes — mapping jalons -> sous-phases 3-6 de la tache 134. Annexe avec pointeurs code (`MetricsController`, `MetricsCollector`, indexes existants `Player.php:18-20` / `Mob.php:16-18`, publishers Mercure, fichiers de config). Mise a jour de `scripts/load-test/README.md` section "Prochaines etapes" pour pointer vers le nouveau document. Aucun changement applicatif (PHP / Twig / migration) : le document est analytique uniquement, les implementations seront livrees en sous-phases ulterieures (3-6) jalon par jalon avec re-run k6 entre chaque. Permet d'ouvrir la phase 3 (Optimisations) avec un plan partage et un ordre d'attaque base sur le ratio effort/gain.
- [~] Optimisations : connection pooling, cache Redis, horizontal scaling plan — plan d'attaque consolide dans `docs/LOAD_TESTING_BOTTLENECKS.md`, jalons A-F a derouler en sous-phases 3-6.
  - [x] **Sous-phase 3f — Cloture analytique du jalon D : `idx_player_map_coords` non actionable** (2026-04-28) : audit du codebase (`grep -rn "coordinates'" src/`, `grep -rn "->andWhere.*coordinates"`, `grep -rn "setParameter.*coordinates"`) confirme **aucune query DQL ne filtre par coordonnees** dans `src/`. Les controllers `/api/map/entities`, `/api/map/cells`, `/api/map/move`, etc. recuperent les acteurs via `findBy(['map' => $map])` (deja servi par l'index existant `idx_player_map` declare sur `Player.php:18`) puis filtrent la proximite en PHP via `abs($ex - $px) > $radius`. Le champ `coordinates` etant stringifie `"x.y"` (CLAUDE.md regle 7), l'index propose `(map_id, coordinates)` ne servirait que pour des queries `WHERE coordinates = 'x.y'` (egalite stricte) qui n'existent pas dans le code. L'alternative BRIN `(map_id)` n'apporte rien sur les cardinalites typiques (1-200 joueurs / carte). Conclusion : **aucun index a ajouter**, le jalon D est cloture par analyse plutot que par migration. Mise a jour de `docs/LOAD_TESTING_BOTTLENECKS.md` : (1) Jalon D titre passe de "partiellement couvert" a "✅ Termine", les 3 cocher `idx_player_map_coords` / `idx_mob_map_alive` / refactor sont desormais coches, le 4eme cocher (profiling EXPLAIN ANALYZE) est documente comme reporte au moment d'un run k6 reel sur staging ; (2) tableau "Etat des jalons" : ligne D passe de 🟡 a ✅ avec mention de la sous-phase 3f ; (3) section "Roadmap a venir" : item 1 (jalon D) marque comme livre, le focus passe a la sous-phase 4 (jalon A Redis). Doc-only, aucun changement applicatif. Prepare l'attaque du jalon A (Redis) en sous-phase 4 prochaine.
  - [x] **Sous-phase 3e — Script `run-all.sh` pour lancer les 4 scenarios k6 sequentiellement** (2026-04-26) : nouveau script bash `scripts/load-test/run-all.sh` (83 lignes, executable) qui itere sur les 4 scenarios `guest-browsing` / `metrics-stress` / `mercure-streaming` / `authenticated-gameplay`, exporte un JSON de resume par scenario via la variable d'env `K6_SUMMARY_EXPORT=last-summary-<name>.json` (reutilise le mecanisme existant de `config.js`), affiche un tableau recapitulatif final (PASS/FAIL/SKIP par scenario), et exit avec un code != 0 egal au nombre de scenarios echoues. SKIP automatique de `authenticated-gameplay` si ni `TEST_USER_EMAIL` ni `TEST_CREDENTIALS_FILE` ne sont definis (fail-fast plutot que silencieux). Detection de `k6` dans le PATH au demarrage avec exit 127 si absent. README mis a jour avec une section "Lancer tous les scenarios d'un coup" + exemples (defauts vs cible Sprint 12 200 VUs/5min staging avec pool de credentials). Prepare l'integration CI nightly mentionnee dans la section "Prochaines etapes (Sprint 12)" du README. Aucun changement applicatif (script bash + doc Markdown). Independant des 14 PR ouvertes.
  - [x] **Sous-phase 3d.doc.b — Pointeurs vers `LOAD_TESTING_BOTTLENECKS.md` + `scripts/load-test/README.md` dans CLAUDE.md** (2026-04-26) : ajoute 2 entrees a la section "Documentation approfondie" du `CLAUDE.md` racine (1 pour le doc analytique des goulots, 1 pour le README des scenarios k6) avec resume des 6 jalons + objectif Sprint 12. Garantit qu'un futur agent (humain ou Claude Code) decouvre l'existence des outils de load testing et du plan d'optimisation au demarrage de session, sans avoir a fouiller `docs/` ou `scripts/`. Doc-only, +2 lignes dans `CLAUDE.md`. Aucun changement applicatif.
  - [x] **Sous-phase 3d.doc — Mise a jour `docs/LOAD_TESTING_BOTTLENECKS.md` avec etat des jalons** (2026-04-26) : ajoute un tableau "Etat des jalons" (A-F) qui synthetise les sous-phases 3a/3b/3c/3d livrees vs ce qui reste a faire, marque le jalon C comme termine et le jalon D comme partiellement couvert (chevauchement `idx_mob_alive_map` + refactor `findByMapWithMonster` deja livres). Renomme la section "Prochaines etapes" en "Avancement & prochaines etapes". Ajoute une section "Resultats observes" (template pour les futures mesures k6 reelles, table date/sous-phase/scenario/p95/notes). Met a jour la roadmap des prochaines sous-phases (3e jalon D residuel, 4 jalon A Redis, 5 jalon B PgBouncer, 6 jalon E Mercure, 7 jalon F scaling). Ajoute des notes "→ Sous-phase XYZ livree" dans les jalons C et D pour pointer vers les sous-phases qui couvrent chaque cocher original. Doc-only, aucun changement applicatif. Diff : ~50 lignes ajoutees / ~30 modifiees dans `docs/LOAD_TESTING_BOTTLENECKS.md` (taille totale 345 lignes <400 budget). Independant des 14 PR ouvertes.
  - [x] **Sous-phase 3d — Partial index `idx_fight_in_progress`** (2026-04-26) : nouvelle migration `Version20260426FightInProgressIndex` (idempotente via `CREATE INDEX IF NOT EXISTS` + `DROP INDEX IF EXISTS` au down) qui ajoute un partial index `mob(in_progress) WHERE in_progress = true` sur la table `fight`. Accelere la requete `count(['inProgress' => true])` execute par `MetricsController::collectGameGauges` pour la gauge `fights_active` (3eme COUNT du hot path `/metrics`, complement des indexes ajoutes en sous-phase 3a pour `players_online` et `mobs_alive`). Partial index plutot qu'index regulier car `in_progress=false` represente >99% des lignes (combats historiques/termines), seul `in_progress=true` est utile a indexer pour la gauge — l'index reste minuscule meme quand la table fight grossit avec l'historique. Pas declare en attribut Doctrine sur l'entite `Fight` (Doctrine ORM ne supporte pas les `WHERE` clauses dans `#[ORM\Index]`) : existe en migration uniquement, comme `idx_mob_alive_map` (sous-phase 3a). Aucun changement applicatif. Combine au cache TTL 10s de la sous-phase 3b, le hot path `/metrics` est desormais optimal sur les 3 gauges (sub-100ms p95 attendue meme sous `metrics-stress` 20 VUs sans think-time, quel que soit le volume DB).
  - [x] **Sous-phase 3c — Suppression du produit cartesien dans `MobRepository::findByMapWithMonster`** (2026-04-26) : la requete utilisee par `/api/map/entities` (mesure par le scenario k6 `authenticated-gameplay`) faisait un `leftJoin` + `addSelect` sur 4 OneToMany imbriques (`mon.spells`, `mon.attack`, `mon.monsterItems`, `mi.item`) qui generaient un produit cartesien sur la wire (3 spells x 5 items = ~15 lignes par mob). Aucun champ de ces OneToMany n'est utilise par le seul appelant `MapApiController::entities()` (qui se contente de `getName()`/`getSlug()` du Monster + champs de Mob). La requete est simplifiee a un seul `join` sur `m.monster` (ManyToOne, sans multiplication). Pour 50 mobs sur une carte, on passe de ~750 lignes hydratees a 50 lignes. Aucun nouveau test : `MapApiEntitiesTest` (functional, deja existant) couvre les assertions du payload (200, presence `players`/`mobs`/`pnjs`, `spriteKey` sur les mobs) qui ne dependent pas des relations supprimees du eager fetch. Aucune migration. Aucun changement de schema. Refactor pur (commentaire de doc-block ajoute pour expliquer l'absence des leftJoins). Premier jalon "code path" du plan d'optimisation `docs/LOAD_TESTING_BOTTLENECKS.md` (apres les jalons indexes 3a + cache 3b qui ciblaient `/metrics`). Re-run `authenticated-gameplay` 200 VUs : reduction attendue de la latence p95 sur `/api/map/entities` proportionnelle au nombre moyen de mobs par carte (gain x10-x15 sur le wire transfer DB).
  - [x] **Sous-phase 3b — Cache TTL des collectors `/metrics` (jalon C complet)** (2026-04-26) : `MetricsController` injecte desormais le pool `cache.app` (`Psr\Cache\CacheItemPoolInterface`, autowire). Le hot path `__invoke()` appelle `maybeCollectGameGauges()` au lieu de `collectGameGauges()` direct : si une cle de fraicheur `metrics_gauges_collected` existe en cache (TTL 10s, constante `GAUGES_FRESHNESS_TTL_SECONDS`), les 3 `COUNT()` Doctrine sur `Player` / `Mob` / `Fight` sont SKIPPES et le rendu Prometheus reutilise les valeurs persistees lors de la derniere collecte (les gauges sont stockees dans `MetricsCollector::saveMetrics` avec un TTL de 24h, donc elles restent affichees meme si la collecte est skippee). Sinon, la collecte tourne et la cle de fraicheur est ecrite avec son TTL. La methode `collectGameGauges()` passe de `private` a `protected` pour permettre l'override par une sous-classe spy dans les tests (aucun impact applicatif : aucune classe externe n'etend `MetricsController`). Decouple completement la frequence de scrape Prometheus (15s par defaut + scrapes externes + runs k6) de la frequence reelle des `COUNT()` DB : sous une charge `metrics-stress` 20 VUs sans think-time, on passe de N requetes/s par worker a 1 requete toutes les 10s globales (gain x100+ sur la charge DB). Avec les 2 indexes ajoutes en sous-phase 3a (`idx_player_updated_at`, `idx_mob_alive_map`), les rares COUNTs restants restent rapides. Tests `MetricsControllerTest` (4 cas via une sous-classe `SpyMetricsController` qui compte les invocations de `collectGameGauges` sans toucher la chaine EM/QueryBuilder/Query) : (1) 1er appel = collecte + cle de fraicheur populated, (2) 2eme appel sous TTL = pas de nouvelle collecte, (3) collecte rejouee apres suppression de la cle, (4) Rendu Prometheus expose correctement les gauges pre-seedees via le collector + Content-Type Prometheus. Aucune migration. Termine le jalon C du plan d'optimisation `docs/LOAD_TESTING_BOTTLENECKS.md` (sous-phase 2d). Prochaine etape : sous-phase 3c (jalon D : indexes composites `idx_player_map_coords` / `idx_mob_map_alive` pour les APIs `/api/map/entities` mesurees par `authenticated-gameplay`).
  - [x] **Sous-phase 3a — Indexes ciblés pour les collectors `/metrics` (jalon C partiel)** (2026-04-26) : nouvelle migration `Version20260426MetricsCollectorIndexes` (idempotente via `CREATE INDEX IF NOT EXISTS` + `DROP INDEX IF EXISTS` au down) qui ajoute 2 indexes utilises par `MetricsController::collectGameGauges`. (1) `idx_player_updated_at` sur `player(updated_at)` accelere `COUNT(p.id) FROM player p WHERE p.updatedAt >= NOW() - 15min` (gauge `players_online`) — declare egalement comme `#[ORM\Index]` sur l'entite `Player` pour rester aligne avec le schema Doctrine genere par `doctrine:schema:create` en environnement de test (pattern miroir des 3 indexes existants `idx_player_map` / `idx_player_fight` / `idx_player_user`). (2) `idx_mob_alive_map` partial index `mob(map_id) WHERE died_at IS NULL` accelere `COUNT(m.id) FROM mob m WHERE m.diedAt IS NULL` (gauge `mobs_alive`) et benefice secondaire pour les requetes de spawn / pathfinding qui filtrent les mobs vivants par carte. Le partial index n'est PAS declare sur l'entite (Doctrine ORM ne supporte pas les `WHERE` clauses dans `#[ORM\Index]`) : il existera en migration uniquement, ce qui est acceptable car les tests utilisent `doctrine:schema:create` (snapshot du schema sans migrations) et n'exercent aucune assertion de plan d'execution. Premier jalon concret du plan d'optimisation `docs/LOAD_TESTING_BOTTLENECKS.md` (jalon C partiel : section "indexes manquants" du jalon 3, livre en avance des autres composants Redis-dependants pour fournir un gain immediat sans nouvelle infrastructure). Re-run `metrics-stress` attendu : reduction du seq scan sur `player.updated_at` et `mob.died_at`. Aucun changement de code applicatif (controller / service / template), aucun nouveau test (pas de logique metier ajoutee). Prepare la sous-phase 3b (jalon A : Redis cache + cache des collectors qui supprimera completement les `COUNT()` du chemin chaud `/metrics`).
- [ ] Objectif : 200 joueurs simultanes sans degradation

### 135 — Localisation i18n (M | ★)
> Prerequis : ∅
> Avancement : sous-phases 1 (selecteur de langue securise), 2a (parite UI FR/EN atteinte), 3a (infrastructure multilingue des noms d'items), 3b (cablage du filter Twig `localized_name` dans les templates shop/inventaire/bestiaire), 3c (fixtures EN pour 35 items de debut de jeu), 3c.b (fixtures EN pour les 26 materia tier 1-3), 3c.c (fixtures EN pour 18 materiaux de craft : ores + lingots + gemmes), 3e.j.c (fixtures EN pour 15 recettes tanneur), 3e.j.d (fixtures EN pour 11 recettes alchimiste), 3e.a (infrastructure multilingue des noms de monstres), 3e.b.a (cablage du filter `localized_monster_name` dans les templates bestiaire/profile/fight), 3e.b.b (fixtures EN pour 24 monstres de niveaux 1-3), 3e.c.d.quest (infrastructure multilingue pour les noms de quetes), 3e.c.d.quest.b (cablage du filter `localized_quest_name` dans les templates du journal de quetes et du dashboard), 3e.c.d.quest.c (fixtures EN pour 26 quetes de debut de jeu), 3e.c.d.quest.d (infrastructure multilingue pour les descriptions de quetes), 3e.c.d.quest.e (cablage du filter `localized_quest_description` dans les templates), 3e.c.d.quest.f (fixtures EN pour 26 descriptions de quetes de debut de jeu), 3e.c.d.quest.g (fixtures EN pour 20 quetes supplementaires : Acte 1 secondaires + chaine Acte 2 "Les Fragments"), 3e.c.d.quest.h (fixtures EN pour 15 quetes supplementaires : Acte 3 La Convergence + dailies + steles de decouverte), 3e.c.d.quest.j (fixtures EN pour les dernieres quetes : puzzle runes + event festival de la lune — 100% des quetes de fixtures traduites), 3e.c.domain (infrastructure multilingue pour les titres d'arbres de talent / domaines), 3e.f (infrastructure multilingue pour les noms et descriptions de races), 3e.g (i18n complet — infra + cablage + fixtures EN — pour les 3 regions du jeu : Plaines de l'Eveil, Sanctuaire de Lumiere, Terres Sauvages), 3e.o (i18n complet pour les 4 festivals saisonniers via cablage de l'API `/api/game/time` consomme par le HUD PixiJS), 2d (i18n du panneau Stimulus de recolte `harvest_controller.js` : 24 cles `game.harvest.*` FR/EN, helper `_label(key, fallback)`, labels passes via `stimulus_controller(...)` dans `templates/game/map/index.html.twig`), 2e (i18n du panneau Stimulus d'inventaire `inventory_controller.js` : 17 cles `game.inventory.{compare,rarity,element}.*` FR/EN, helper `_label(key, fallback)`, reuse des 12 cles existantes `game.inventory.slot.*`, labels passes via `stimulus_controller(...)` dans `templates/game/inventory/index.html.twig`), 2f (i18n du panneau Stimulus de slot materia `materia_slot_controller.js` : 7 cles `game.inventory.materia_slot.*` FR/EN + 3 cles d'elements supplementaires `metal/beast/none` ajoutees a `game.inventory.element.*`, helper `_label(key, fallback)`, reuse des 6 elements deja livres en 2e, labels passes via `stimulus_controller(...)` dans `templates/game/inventory/equipment/_materia_track.html.twig`) 2g (i18n du panneau Stimulus de file de craft `craft_queue_controller.js` : 4 cles `game.crafting.queue.*` FR/EN, helper `_label(key, fallback)`, labels passes via `stimulus_controller(...)` dans `templates/game/crafting/_recipe_card.html.twig`) 2h (i18n de la barre d'action rapide Stimulus `quickbar_controller.js` : 4 cles `game.quickbar.*` FR/EN — `aria_label/item_unavailable/error/network_error`, helper `_label(key, fallback)`, labels passes via `stimulus_controller(...)` dans `templates/game/map/index.html.twig`) 2i (i18n des notifications Mercure d'influence guilde `influence_notification_controller.js` : 7 cles `game.influence_notification.*` FR/EN, helper `_label(key, fallback)`, labels passes via `stimulus_controller(...)` dans `templates/game/game.html.twig`) 3e.r (i18n complet de la page de soutien `/game/support` : 19 cles `game.support.*` FR/EN couvrant titre, header, description marketing, sections donation/communaute/que financent les dons, badge "aucun avantage en jeu", footer transparence + lien retour) et 3e.s (i18n des pages de selection / limite de personnages `character/{select,limit_reached}.html.twig` : 6 cles `game.character.{select,limit_reached}.*` FR/EN, paragraphe avec parametres `%count%` + `%plural%`) livrees.
- [x] Extraction des chaines via Symfony Translation — base existante sous `translations/messages.{fr,en}.json` (format JSON arborescent, deja exploite par `|trans`). Non re-extrait en XLIFF (choix projet de conserver JSON).
- [~] Traduction EN prioritaire (UI, items, quetes, dialogues) — sous-phase 2a livree 2026-04-21
  - [x] **2a — Parite de cles FR/EN sur l'UI** (2026-04-21) : audit systematique de `messages.{fr,en}.json` via flatten + diff ensembliste revele 19 cles presentes en FR mais absentes en EN. Ajout cible : `game.nav.bestiary` + `game.nav.craft` (entrees de navigation) et l'integralite du namespace `game.bestiary.*` (17 cles : `title`, `subtitle`, `discovered`, `total_kills`, `kills`, `tier_weaknesses`, `tier_loot`, `tier_title`, `weaknesses`, `loot_table`, `loot_probability`, `next_tier`, `empty`, `empty_hint`, `boss`, `level`, `difficulty`). Resultat : 432 cles FR = 432 cles EN, ecart nul. Impact immediat : la page `/game/bestiary` et les liens de navigation ne tombent plus sur le fallback de cle brute en anglais.
  - [ ] **2b** — audit des cles utilisees mais absentes de `messages.*.json` (via `bin/console debug:translation en`)
  - [ ] **2c** — traduction EN des items, quetes, dialogues PNJ (contenu de jeu, necessite systeme dedie cf. sous-phase 4)
  - [x] **3e.s — i18n des pages de selection / limite de personnages** (2026-04-28) : 2eme livraison de la serie 3e dediee aux pages Twig statiques (apres 3e.r `/game/support`). Couvre 2 templates lies a la gestion multi-personnages (CLAUDE.md regle 12 : 1 personnage par compte par defaut, configurable, plusieurs cas particuliers). (1) `templates/game/character/select.html.twig` (77 lignes) : ecran de selection du personnage actif a la connexion. 3 chaines extraites — `{% block title %}Choisir un personnage{% endblock %}`, h1 "Choisir un personnage", subtitle "Sélectionnez le personnage avec lequel vous souhaitez jouer". (2) `templates/game/character/limit_reached.html.twig` (27 lignes) : ecran d'erreur affiche quand le joueur tente de creer plus de personnages que la limite autorisee. 4 chaines extraites — `{% block title %}Limite de personnages atteinte{% endblock %}`, h1 "Limite atteinte", paragraphe avec parametre dynamique `Vous avez atteint la limite de %count% personnage%plural% par compte.` (le suffixe `%plural%` re-utilise la meme logique de pluralisation FR que celle existante dans le template — passee via `{'%count%': maxPlayers, '%plural%': maxPlayers > 1 ? 's' : ''}` au filter `|trans`), bouton "Retour au jeu". 6 cles ajoutees sous `game.character.{select.title,select.subtitle,limit_reached.title_full,limit_reached.title_short,limit_reached.body,limit_reached.back}`. La cle `game.character.select.title` est utilisee 2 fois dans `select.html.twig` (block title + h1), economisant un doublon. Parite maintenue : 545 cles FR = 545 cles EN. Aucune migration, aucun changement backend, pas de nouveau test (pages statiques sans logique). Impact immediat : basculer la locale en EN affiche `Choose a character` / `Select the character you wish to play` sur `/character/select`, et `Character limit reached` / `Limit reached` / `You have reached the limit of N characters per account.` / `Back to the game` sur `/character/limit-reached`. Sous-phase independante des 14 PR i18n / avatar / mounts / events en vol : aucune ne touche `templates/game/character/{select,limit_reached}.html.twig`, zero risque de conflit.
  - [x] **3e.r — i18n complet de la page de soutien (`/game/support`)** (2026-04-28) : nouvelle sous-phase qui ouvre l'i18n des **pages Twig statiques** (apres la cloture de la serie 2 dediee aux controleurs Stimulus). Le template `templates/game/support.html.twig` (126 lignes) avait 19 chaines FR hardcodees couvrant l'integralite de la page : `{% block title %}Soutenir le projet{% endblock %}`, h1 "Soutenir Amethyste", subtitle "Aidez-nous a faire vivre le projet", description marketing avec emphasis "100% gratuit" + sans pay-to-win, badge "aucun avantage en jeu" avec emphasis, h2 "Faire un don" / "Rejoindre la communaute" / "A quoi servent les dons ?", subtitles Ko-fi/GitHub/Discord, 3 cartes "Serveurs/Contenu/Perennite" avec descriptions, footer "Amethyste est un projet independant" + lien "← Retour au jeu". 19 chaines extraites vers `game.support.{page_title,heading,subtitle,description_html,no_advantage_html,section_donate,kofi_subtitle,github_subtitle,section_community,discord_subtitle,section_what_for,servers,servers_desc,content,content_desc,perennity,perennity_desc,transparency,back_to_game}`. Les 2 chaines avec emphasis HTML inline (`description_html` + `no_advantage_html`) preservent les `<strong class="text-purple-400">` / `<strong class="text-gray-300">` via le filter `|raw` apres `|trans`, conserve la mise en forme exacte du rendu FR. Aucune migration, aucun changement backend, pas de nouveau test (page statique sans logique). Parite maintenue : 539 cles FR = 539 cles EN. Impact immediat : basculer la locale en EN via `/game/settings` affiche `Support the project` (titre), `Help us keep the project alive`, `Make a donation`, `Join the community`, `What do donations fund?`, `Servers/Content/Perennity` au lieu des strings FR. Sous-phase independante des 14 PR i18n / avatar / mounts / events en vol : aucune ne touche `templates/game/support.html.twig`, zero risque de conflit.
  - [x] **2i — i18n des notifications Mercure d'influence guilde (`influence_notification_controller.js`)** (2026-04-28) : suite directe des sous-phases 1c/2d/2e/2f/2g/2h (i18n des controleurs Stimulus de gameplay) qui applique le meme pattern aux toasts de notifications d'influence + controle de cite (Sprint 6 / Plan GCC). Le controleur Stimulus `assets/controllers/influence_notification_controller.js` (112 lignes) declare desormais `static values = { mercureUrl, guildId, labels }` et lit chaque label via le helper defensif `_label(key, fallback)`. 7 chaines FR hardcodees deplacees vers les cles `game.influence_notification.{influence_word,overtook_in,challenge_completed,points_influence,no_guild,takes_control_of,season}` qui couvrent les 4 types d'evenement Mercure : (1) `influence_awarded` -> "+%pts% influence (...)" ; (2) `influence_overtake` -> "%guild% [%tag%] vous a depasse en %region%" ; (3) `challenge_completed` -> "Defi complete : ..." ; (4) `city_control_change` -> "%guild% prend le controle de %region% (Saison %N%)". Le template `templates/game/game.html.twig` remplace `data-controller="influence-notification" data-...-value=...` par `{{ stimulus_controller('influence-notification', { mercureUrl, guildId, labels: { 7 cles 'game.influence_notification.*'|trans } }) }}` (les 2 valeurs `mercureUrl` + `guildId` sont passees via le helper Twig au lieu d'attributs `data-*` separes — meme rendu DOM final). Parite maintenue : 520 cles FR = 520 cles EN. Aucune migration, aucun changement backend (les Mercure publishers continuent a envoyer les memes payloads `pointsEarned/regionName/...`), pas de nouveau test (le comportement client reste identique en FR avec fallbacks ; le rendu EN est piloté par `RequestStack::getCurrentRequest()->getLocale()` via `|trans`). Impact immediat : basculer la locale en EN via `/game/settings` affiche `+5 influence (Quest reward) — Plains of the Awakening`, `[GUILD-A] [TAG] overtook you in Sanctuary of Light !`, `Challenge completed : "Daily run" (+10 influence pts)`, `🏰 No guild takes control of Wild Lands (Season 3)` au lieu des strings FR sur les toasts d'influence. Sous-phase independante des 14 PR i18n / avatar / mounts / events en vol : aucune ne touche `influence_notification_controller.js` ni le bloc Twig de `game.html.twig` qui l'instancie, zero risque de conflit.
  - [x] **2h — i18n de la barre d'action rapide Stimulus (`quickbar_controller.js`)** (2026-04-28) : suite directe des sous-phases 1c/2d/2e/2f/2g (i18n des controleurs `fishing` / `harvest` / `inventory` / `materia_slot` / `craft_queue`) qui applique le meme pattern a la barre d'action rapide affichee en bas de la carte (`/game/map`). Le controleur Stimulus `assets/controllers/quickbar_controller.js` (287 lignes) declare desormais `static values = { labels: { type: Object, default: {} } }` et lit chaque label via le helper defensif `_label(key, fallback)`. 3 chaines FR hardcodees deplacees vers les cles `game.quickbar.{item_unavailable,error,network_error}` : (1) `'Objet indisponible'` (toast quand l'objet n'est plus utilisable, ex: quantite 0) -> `item_unavailable`, (2) `'Erreur'` (fallback de message d'erreur si le serveur n'en fournit pas) -> `error`, (3) `'Erreur réseau'` (toast d'echec de la requete fetch) -> `network_error`. Le template `templates/game/map/index.html.twig` remplace `data-controller="quickbar"` par `{{ stimulus_controller('quickbar', { labels: { 3 cles 'game.quickbar.*'|trans } }) }}` et l'aria-label `"Barre d'action rapide"` est aussi extrait via `'game.quickbar.aria_label'|trans` (4eme cle ajoutee). Parite maintenue : 513 cles FR = 513 cles EN. Aucune migration, aucun changement backend, pas de nouveau test (le comportement client reste identique en FR avec fallbacks ; le rendu EN est piloté par `RequestStack::getCurrentRequest()->getLocale()` via `|trans`). Impact immediat : basculer la locale en EN via `/game/settings` affiche `Quick action bar` (aria), `Item unavailable`, `Error`, `Network error` au lieu des strings FR sur la barre d'action rapide. Sous-phase independante des 14 PR i18n / avatar / mounts / events en vol : aucune ne touche `quickbar_controller.js` ni l'aria-label de la barre, zero risque de conflit.
  - [x] **2g — i18n du panneau Stimulus de file de craft (`craft_queue_controller.js`)** (2026-04-28) : suite directe des sous-phases 1c/2d/2e/2f (i18n des controleurs `fishing` / `harvest` / `inventory` / `materia_slot`) qui applique le meme pattern au bouton de fabrication batch des recettes. Le controleur Stimulus `assets/controllers/craft_queue_controller.js` (86 lignes) declare desormais `static values = { ..., labels: { type: Object, default: {} } }` et lit chaque label via le helper defensif `_label(key, fallback)`. 4 chaines FR hardcodees deplacees vers les cles `game.crafting.queue.{craft_button,crafting_progress,crafted_done,connection_error}` : (1) `'Fabriquer'` (label du bouton apres echec ou retour) -> `craft_button`, (2) `'Fabrication...'` (label pendant la requete en cours) -> `crafting_progress`, (3) `'fabriques !'` (suffixe affiche apres succes : `${crafted}/${quantity} fabriques !`) -> `crafted_done`, (4) `'Erreur de connexion.'` (toast d'erreur reseau) -> `connection_error`. Le template `templates/game/crafting/_recipe_card.html.twig` remplace `data-controller="craft-queue" data-craft-queue-slug-value=... data-craft-queue-max-value=...` par `{{ stimulus_controller('craft-queue', { slug: recipe.slug, max: maxCraftQty, labels: { 4 cles 'game.crafting.queue.*'|trans } }) }}` (les 2 valeurs `slug` + `max` sont desormais passees via le helper Twig au lieu d'attributs `data-*` separes — meme rendu DOM). Parite maintenue : 509 cles FR = 509 cles EN. Aucune migration, aucun changement backend, pas de nouveau test (le comportement client reste identique en FR avec fallbacks ; le rendu EN est piloté par `RequestStack::getCurrentRequest()->getLocale()` via `|trans`). Impact immediat : basculer la locale en EN via `/game/settings` affiche `Crafting...`, `crafted!`, `Craft`, `Connection error.` au lieu des strings FR sur les boutons de craft. Sous-phase independante des 14 PR i18n / avatar / mounts / events en vol : aucune ne touche `craft_queue_controller.js` ni `_recipe_card.html.twig` (PR #444 audit i18n n'introduit aucune cle ; PR #451/472/455/464/458/456/529/522 ciblent les entites name/description, pas les controleurs UI), zero risque de conflit.
  - [x] **2f — i18n du panneau Stimulus de slot materia (`materia_slot_controller.js`)** (2026-04-28) : suite directe des sous-phases 1c/2d/2e (i18n des controleurs `fishing` / `harvest` / `inventory`) qui applique le meme pattern au panneau de tooltip + bottom-sheet des slots materia FF7-style. Le controleur Stimulus `assets/controllers/materia_slot_controller.js` declare desormais `static values = { labels: { type: Object, default: {} } }` et lit chaque label via le helper defensif `_label(key, fallback)`. 7 chaines FR hardcodees deplacees vers les cles `game.inventory.materia_slot.{empty_slot,element_required,level_short,max_level,spell_label,bonus_xp,bonus_synergy}`. La map interne `_elementLabel(e)` est refactoree pour appeler `this._label('element.X', fallback)` qui **reutilise** les 6 cles `game.inventory.element.{fire,water,earth,air,light,dark}` livrees en sous-phase 2e ; 3 nouvelles cles d'elements specifiques materia (`metal`, `beast`, `none` -> `—`) sont **ajoutees** au meme namespace pour completer la couverture (les 3 elements absents de l'inventaire d'equipement standard mais presents dans le systeme materia avance). Parite maintenue : 505 cles FR = 505 cles EN. Le template `templates/game/inventory/equipment/_materia_track.html.twig` remplace `data-controller="materia-slot"` par `{{ stimulus_controller('materia-slot', { labels: { ... 16 cles 'game.inventory.{materia_slot,element}.*'|trans } }) }}`. Aucune migration, aucun changement backend, pas de nouveau test (le comportement client reste identique en FR avec fallbacks ; le rendu EN est piloté par `RequestStack::getCurrentRequest()->getLocale()` via `|trans`). Impact immediat : basculer la locale du compte en EN via `/game/settings` affiche `Empty slot`, `Required element:`, `Lv.`, `MAX`, `Spell:`, `+25% XP — Elemental bonus`, `+15% Damage — Linked synergy`, `Metal/Beast/—` au lieu des strings FR sur les tooltips de slot materia (paper doll inventaire, ecran de modification d'equipement). Sous-phase independante des 14 PR i18n / avatar / mounts / events en vol : aucune ne touche `materia_slot_controller.js`, `_materia_track.html.twig` ni les nouvelles cles, zero risque de conflit.
  - [x] **2e — i18n du panneau Stimulus d'inventaire (`inventory_controller.js`)** (2026-04-28) : suite directe des sous-phases 1c (i18n `fishing_controller.js` du mini-jeu de peche, livree 2026-04-24) et 2d (i18n `harvest_controller.js`, livree 2026-04-28) qui applique le meme pattern au panneau d'inventaire (item tooltip desktop + bottom-sheet mobile + comparaison item equipe vs item inventaire). Le controleur Stimulus `assets/controllers/inventory_controller.js` declare desormais `static values = { labels: { type: Object, default: {} } }` et lit chaque label via le helper defensif `_label(key, fallback)`. 5 chaines FR hardcodees dans la section "Compare" (`'Niveau '`, `'Slots materia'` (x2), `'Perd '`, `'Statistiques identiques'`, `'Slot vide'`) deplacees vers les cles `game.inventory.compare.{level,materia_slots,loses,identical_stats,empty_slot}`. 3 maps internes (`_rarityLabel` / `_slotLabel` / `_elementLabel`) refactorees pour appeler `this._label('rarity.X', fallback)` / `this._label('slot.X', fallback)` / `this._label('element.X', fallback)` avec fallbacks FR conserves localement. Les **12 cles de slot** existantes `game.inventory.slot.*` (head, neck, chest, shoulder, hand, main_weapon, side_weapon, belt, leg, foot, ring_1, ring_2 — livrees lors de la sous-phase 2a 2026-04-21) sont **reutilisees** sans nouvelle creation. **17 nouvelles cles** ajoutees sous `game.inventory.{compare,rarity,element}.*` (5 compare + 6 rarity + 6 element). Parite maintenue : 495 cles FR = 495 cles EN. Le template `templates/game/inventory/index.html.twig` remplace `data-controller="inventory"` par `{{ stimulus_controller('inventory', { labels: { ... 30 cles `'game.inventory.*'|trans` } }) }}`. Aucune migration, aucun changement backend, pas de nouveau test (le comportement client reste identique en FR avec fallbacks ; le rendu EN est piloté par `RequestStack::getCurrentRequest()->getLocale()` via `|trans`). Impact immediat : basculer la locale du compte en EN via `/game/settings` affiche `Level`, `Materia slots`, `Loses`, `Identical stats`, `Empty slot`, `Amethyst/Legendary/Epic/Rare/Uncommon/Common`, `Fire/Water/Earth/Air/Light/Dark` au lieu des strings FR sur la tooltip + bottom-sheet d'inventaire. Sous-phase independante des 14 PR i18n / avatar / mounts / events en vol : aucune ne touche `inventory_controller.js`, `templates/game/inventory/index.html.twig` ni les nouvelles cles `game.inventory.{compare,rarity,element}.*`, zero risque de conflit.
  - [x] **2d — i18n du panneau Stimulus de recolte (`harvest_controller.js`)** (2026-04-28) : suite directe de la sous-phase 1c (i18n `fishing_controller.js` du mini-jeu de peche, livree 2026-04-24) qui applique le meme pattern au panneau de recolte. 17 chaines FR hardcodees dans `assets/controllers/harvest_controller.js` + 1 aria-label dans `templates/game/map/index.html.twig` deplacees vers le bundle de traductions. Le controleur Stimulus declare desormais `static values = { labels: { type: Object, default: {} } }` et lit chaque label via un helper defensif `_label(key, fallback)` qui retombe sur le fallback FR si la valeur est absente (retrocompatibilite forte). Le template pousse les labels au controleur via `stimulus_controller('harvest', { labels: { ... } })` avec 19 cles `'game.harvest.*'|trans` + l'aria-label rendu serveur. Nouvelles cles FR/EN groupees sous `game.harvest.*` (24 cles x 2 langues = parite 478 cles FR = 478 cles EN maintenue) : `panel_aria`, `spot_fallback`, `load_error`, `connection_error`, `harvesting`, `cannot_harvest`, `empty_result`, `success`, `tool_broken`, `skill_required`, `skill_missing`, `skill_insufficient`, `ready`, `respawn` + 5 cles `tool_required.{pickaxe,sickle,fishing_rod,skinning_knife,default}` + 5 cles `action.{pickaxe,sickle,fishing_rod,skinning_knife,default}`. Aucune migration, aucun changement backend, pas de nouveau test (le comportement client reste identique en FR avec fallbacks, et le rendu EN est piloté par `RequestStack::getCurrentRequest()->getLocale()` via `|trans`). Impact immediat : basculer la locale du compte en EN via `/game/settings` affiche `Mine` / `Harvest` / `Fish` / `Skin` / `Cannot harvest here.` / `Tool required` etc. au lieu des strings FR sur le panneau de recolte. Sous-phase independante des 14 PR i18n / avatar / mounts / events en vol : aucune ne touche `harvest_controller.js`, `templates/game/map/index.html.twig` ni les cles `game.harvest.*`, zero risque de conflit.
- [x] Selecteur de langue dans les parametres joueur — `framework.yaml` declare `enabled_locales: ['fr', 'en']` ; `LocaleController` valide strictement la locale contre cette whitelist (fallback `default_locale` si inconnue), bloque les payloads exotiques via contrainte regex de route, verifie que le referer pointe vers le meme host avant redirection. `/game/settings` expose un `<select>` branche a un controller Stimulus `locale-switcher` qui redirige vers `/change-locale/{locale}` au changement ; l'option selectionnee reflete la locale courante (`app.request.locale`). Tests unitaires (`LocaleControllerTest`, 6 cas) couvrent les chemins valide / invalide / payload malveillant / referer safe-or-not.
- [~] Contenu de jeu multilingue (noms items, descriptions sorts) — sous-phases 3a, 3b et 3c livrees (infra + cablage + fixtures EN starter)
  - [x] **3a — Infrastructure multilingue pour les noms d'items** (2026-04-21) : colonne JSON `name_translations` ajoutee sur `game_items` (migration `Version20260421ItemNameTranslations`, idempotente). Entite `Item` etendue avec `getLocalizedName(?string $locale): string` (fallback gracieux sur `$this->name` si locale nulle/vide, colonne nulle, locale absente ou valeur blanche), `getNameTranslations(): array` (defaut `[]`) et `setNameTranslations(?array $translations): Item` avec normalisation (cles/valeurs vides filtrees, stockage compact en `null` si aucune entree valide). Tests `ItemLocalizationTest` (7 cas). Aucun template ni controller modifie : les sous-phases 3b / 3c cableront progressivement.
  - [x] **3b — Cablage dans les templates/helpers** (2026-04-21) : nouveau filter Twig `localized_name` (extension `App\Twig\ItemLocalizationExtension`) qui applique `Item::getLocalizedName` avec la locale courante recuperee depuis `RequestStack` (fallback transparent sur `Item::name` si RequestStack vide, Item null ou traduction manquante). Templates cables avec `{{ item|localized_name }}` : `templates/game/shop/index.html.twig` (listes acheter/vendre), `templates/game/inventory/items_list.html.twig`, `templates/game/inventory/materia/_slot_select.html.twig`, `templates/game/inventory/materia/_slot_select_embed.html.twig`, `templates/game/bestiary/index.html.twig` (table de loot affichee aux paliers de decouverte). Les controllers `Game\Inventory\ItemsController` et `Game\Inventory\MateriaController` (qui construisent des tableaux associatifs pour leurs templates) recuperent la locale via `Request::getLocale()` et appellent `Item::getLocalizedName($locale)` directement. Les attributs `data-item-name` et `data-item-eq-name` (utilises par le JS de recherche) conservent volontairement `item.name` non traduit pour preserver la semantique de cle. Tests `ItemLocalizationExtensionTest` (5 cas : traduction matchee, fallback base name, RequestStack vide, Item null, enregistrement du filter). Une fois la sous-phase 3c (fixtures EN pour les noms d'items) livree, chaque paire `{fr, en}` sera automatiquement affichee dans les 5 templates cables sans redeploiement de template.
  - [x] **3c — Fixtures EN pour les noms des items de debut de jeu** (2026-04-22) : `ItemFixtures` accepte desormais la cle optionnelle `name_translations` (array) dans chaque entree du tableau d'items, qui est passee a `Item::setNameTranslations()` lors de la construction. 35 items de debut de jeu recoivent une traduction EN : equipements starter (short_sword=Short Sword, long_sword=Long Sword, leather_boots=Leather Boots, leather_hat=Leather Hat, leather_armor=Leather Armor), consommables / potions (life_potion=Healing Potion, healing_potion_small=Minor Healing Potion, healing_potion_medium=Healing Potion, healing_potion_major=Major Healing Potion, energy_potion_small=Minor Energy Potion, antidote=Antidote, bread=Bread, grilled_meat=Grilled Meat, stew=Stew), parchemins (scroll_teleport=Teleport Scroll, scroll_xp_boost=Scroll of Knowledge, scroll_identification=Identification Scroll, ancient_scroll=Ancient Scroll, life_domain_parchment=Healing Apprenticeship, miner_domain_parchment=Mining Discovery), outils (fishing_rod=Fishing Rod, pickaxe=Pickaxe), equipement tier 1 (iron_sword=Iron Sword, wooden_shield=Wooden Shield, leather_helmet=Leather Helmet, magic_amulet=Magic Amulet, magic_ring=Magic Ring, bow=Bow, staff=Staff, dagger=Dagger), divers (mushroom=Mushroom, beer_pint=Beer Mug, wood_log=Wood Log, herb_mint=Wild Mint, herb_lavender=Lavender). Les items restants (materia tier 1+, equipements tier 2+, gemmes, recettes craft, fragments Acte 2+) conservent leur nom FR comme fallback (couvert dans une sous-phase ulterieure). Aucune migration necessaire : la colonne JSON `name_translations` est deja creee par la sous-phase 3a. Zero impact FR (le fallback transparent de `Item::getLocalizedName` preserve le rendu actuel) ; impact EN immediat sur `/game/shop`, `/game/inventory`, `/game/bestiary` (table de loot) et les selecteurs de materia (via le filter `localized_name` cable en 3b).
  - [x] **3c.u — Fixtures EN pour les 28 equipements elementaires tier 2 (4 elements x 7 slots)** (2026-04-27) : suite directe de la sous-phase 3c.t qui couvre l'integralite du palier d'equipement tier 2 elementaire (level 5, rarete Uncommon, +10% degats elementaires). 4 elements (Fire/Water/Earth/Air) x 7 slots (sword main_weapon, shield side_weapon, helmet head, chest, legs, boots, gloves) = 28 items : `t2-fire-sword=Blazing Sword`, `t2-water-sword=Frost Sword`, `t2-earth-sword=Telluric Sword`, `t2-air-sword=Wind Sword`, `t2-fire-shield=Blazing Shield`, `t2-water-shield=Frost Shield`, `t2-earth-shield=Stone Shield`, `t2-air-shield=Wind Shield`, `t2-fire-helmet=Blazing Helm`, `t2-water-helmet=Frost Helm`, `t2-earth-helmet=Stone Helm`, `t2-air-helmet=Wind Helm`, `t2-fire-chest=Blazing Cuirass`, `t2-water-chest=Frost Cuirass`, `t2-earth-chest=Stone Cuirass`, `t2-air-chest=Wind Cuirass`, `t2-fire-legs=Blazing Greaves`, `t2-water-legs=Frost Greaves`, `t2-earth-legs=Stone Greaves`, `t2-air-legs=Wind Greaves`, `t2-fire-boots=Blazing Boots`, `t2-water-boots=Frost Boots`, `t2-earth-boots=Stone Boots`, `t2-air-boots=Wind Boots`, `t2-fire-gloves=Blazing Gauntlets`, `t2-water-gloves=Frost Gauntlets`, `t2-earth-gloves=Stone Gauntlets`, `t2-air-gloves=Wind Gauntlets`. +28 lignes ajoutees a `ItemFixtures.php` (4339 au total, respecte la limite de 50 lignes ajoutees a un fichier >400 lignes). Aucune migration, aucun nouveau test : infrastructure (3a) + cablage Twig (3b) deja en place, `ItemLocalizationTest` couvre deja la mecanique. Impact EN immediat sur `/game/shop` (forgerons), `/game/inventory` (paper doll + onglets equipement / items) et l'ecran de craft via le filter `localized_name` cable en 3b. Zero impact FR (fallback transparent preserve). Items restants sans traduction : 28 equipements elementaires t3 (Metal/Bete/Lumiere/Ombre x 7 slots) + 2 cosmetiques (cape lunaire, couronne lunaire). Resultat : 249/279 items traduits = 89%.
  - [x] **3c.t — Fixtures EN pour 7 bijoux haut tier (gold/mithril/prismatic) + 4 masterwork artisanaux (11 items)** (2026-04-26) : suite directe de la sous-phase 3c.s qui couvre 7 bijoux haut tier (`gold_ring=Jeweled Gold Ring`, `gold_amulet=Gold Amulet`, `gold_crown=Gold Crown`, `mithril_ring_jewel=Jeweled Mithril Ring`, `mithril_amulet=Mithril Amulet`, `legendary_ring=Prismatic Ring`, `legendary_amulet=Prismatic Amulet`) + les 4 chefs-d'oeuvre exclusifs aux maitres artisans (task 122) (`masterwork_blade=Master Blacksmith's Blade`, `masterwork_drakehide_cloak=Master Leatherworker's Cloak`, `masterwork_grand_elixir=Master Alchemist's Grand Elixir`, `masterwork_starforged_ring=Master Jeweler's Ring`). Couvre les recompenses de progression intermediaire (level 6-18) du joaillier + les chefs-d'oeuvre legendaires des 4 metiers d'artisan, visibles dans `/game/inventory` (paper doll + onglet items), `/game/shop` si exposes, et l'ecran de craft via le filter `localized_name` cable en 3b. +11 lignes ajoutees a `ItemFixtures.php`. Aucune migration, aucun nouveau test (`ItemLocalizationTest` couvre deja la mecanique). Items restants sans traduction : equipements elementaires t2/t3 par slot (~56 items repartis sur les 4 elements feu/givre/pierre/vent x slots cuirass/jambieres/bottes/casque/bouclier/gantelets), 18 items boss zone Acte 2 (peaux ancestrales, lame, bouclier divers), cosmetiques cape/couronne lunaire (2). Resultat : 221/279 items traduits = 79%.
  - [x] **3c.s — Fixtures EN pour 9 items iron tier + leather skins (accessoires & bijoux artisans)** (2026-04-26) : suite directe de la sous-phase 3c.r qui couvre 7 items du palier fer/cuivre + 2 peaux de cuir brutes : `leather_skin_1=Leather Hide`, `leather_skin_2=Leather Hide` (materiaux tannerie utilises en craft), `iron_dagger=Iron Dagger` (arme craft soldier), `iron_shield=Iron Shield` (bouclier craft), `iron_helmet=Iron Helmet` (casque craft, level 8 Uncommon), `copper_ring=Copper Ring` (anneau bas niveau), `iron_ring=Iron Ring`, `iron_amulet=Iron Amulet`, `iron_bracelet=Iron Bracelet` (3 bijoux joaillier base). Couvre les recompenses craft de debut/milieu de jeu visibles dans `/game/inventory` (onglet equipement + items), `/game/shop` (forgerons et joailliers) et l'ecran de craft via le filter `localized_name` cable en 3b. +9 lignes ajoutees a `ItemFixtures.php`. Aucune migration, aucun nouveau test. Items restants sans traduction : equipements elementaires t2/t3 par slot (~64), bijoux gold/mithril/legendary (8), masterwork (4), cosmetiques (2).
  - [x] **3c.r — Fixtures EN pour 4 boss de zone Acte 2 + 3 items magiques varies (7 items)** (2026-04-26) : suite directe de la sous-phase 3c.q (boss legendaries Acte 1) qui couvre 4 drops uniques des boss de zone (tache 66) : `guardian_bark_armor=Ancestral Bark Cuirass` (cuirasse Forest Guardian, Beast), `guardian_thorn_staff=Primordial Thorn Staff` (baton druid, Earth), `forgelord_obsidian_blade=Forge Lord's Obsidian Blade` (epee Forge Lord, Metal), `forgelord_dark_plate=Dark Forge Plate` (plastron Forge Lord, Dark) + 3 items magiques varies orphelins : `magic_crystal=Magic Crystal` (boost element aleatoire, Epic), `herbalist_domain_parchment=Herbalism Discovery` (apprentissage du domaine herbalist), `poison_vial=Poison Vial` (consommable alchimiste). +7 lignes ajoutees a `ItemFixtures.php`. Aucune migration, aucun nouveau test. 20 items restent sans traduction.
  - [x] **3c.q — Fixtures EN pour 6 recompenses uniques de boss (rarete Legendary)** (2026-04-26) : suite directe de la sous-phase 3c.p (starter equipment) qui couvre 6 drops legendaires uniques de boss tier 4-5 : `dragon_fang_blade=Dragon Fang Blade` (epee Dragon ancestral, element Fire), `dragon_scale_armor=Dragon Scale Armor` (plastron Dragon, Fire), `griffin_talon_ring=Griffin Talon Ring` (anneau Griffin, Air), `minotaur_horn_helm=Minotaur Horn Helm` (heaume Minotaure alpha, Earth), `golem_heart_shield=Golem Heart Shield` (bouclier Golem de pierre, Earth), `troll_king_belt=Troll King Belt` (ceinture Troll). Tous level 25+, rarete `Legendary`, 2 slots materia. Couvre les drops uniques affiches sur la table de loot du bestiaire et l'inventaire post-combat. +6 lignes ajoutees a `ItemFixtures.php`. Aucune migration, aucun nouveau test. 27 items restent sans traduction (boss zone tier 5+, items magiques exotiques, equipements elementaires t1+ par slot).
  - [x] **3c.p — Fixtures EN pour 7 equipements starter (set complet de debut de jeu)** (2026-04-26) : suite directe de la sous-phase 3c.o (consommables) qui couvre le **set complet d'equipement de demarrage** que tout nouveau personnage acquiert au tutoriel : 1 arme (`wooden_sword=Wooden Sword`) + 6 pieces d'armure (`starter_helmet=Rusty Helmet`, `starter_chest=Padded Tunic`, `starter_legs=Cloth Leggings`, `starter_boots=Worn Sandals`, `starter_gloves=Work Gloves`, `starter_shield=Wooden Shield`). Tous element=None (sans prerequis), niveau 1, 1 slot materia chacun. Couvre l'ecran de creation de personnage + premier inventaire post-tutoriel + paper doll. +7 lignes ajoutees a `ItemFixtures.php`. Aucune migration, aucun nouveau test. 33 items restent sans traduction (equipements elementaires t1+, items boss legendaires, etc.).
  - [x] **3c.o — Fixtures EN pour 7 consommables alchimistes + nourriture (4 elixirs + 3 food)** (2026-04-26) : suite directe de la sous-phase 3c.n (100% materias) qui ouvre la categorie consommables. Couvre 4 elixirs alchimistes (`elixir_force=Strength Elixir`, `elixir_defense=Defense Elixir`, `onguent_healing=Healing Salve`, `elixir_vitality=Vitality Elixir`) + 3 nourritures (`food_bread=Bread`, `food_cheese=Cheese`, `food_apple=Apple`). Ces items sont consommes en jeu (restore_energy / use_spell), donc visibles dans `/game/inventory` onglet items, le shop alchimiste, les drops de quetes/monstres, et l'ecran de combat. +7 lignes ajoutees a `ItemFixtures.php`. Aucune migration, aucun nouveau test. 40 items restent sans traduction (equipements elementaires t2/t3 par slot, items boss legendaires, divers consommables avances).
  - [x] **3c.n — Fixtures EN pour les 6 dernieres materias Eau/Metal/Bete — 100% materias traduites** (2026-04-26) : extension finale de la sous-phase 3c.m qui couvre les 6 dernieres materias non traduites : 2 Eau (`materia_tidal_wave=Tidal Wave` m3 — hydromancer, `materia_ice_lance=Ice Lance` m2), 2 Metal (`materia_iron_fist=Iron Fist` m2 — soldier, `materia_metal_storm=Metal Storm` m3 — knight) et 2 Bete (`materia_thorn_burst=Thorn Burst` m2 — druid, `materia_leaf_blade=Leaf Blade` m3 — hunter). **Atteint 100% de parite FR/EN sur l'integralite des materias** (verification awk : 0 materia sans `name_translations` cote `ItemFixtures::$items` racine). +6 lignes ajoutees a `ItemFixtures.php`. Aucune migration, aucun nouveau test. Impact EN immediat sur `/game/inventory` et `/game/shop` pour TOUS les selecteurs de materia, peu importe le domaine du joueur ou le niveau de progression. 47 items restent sans traduction (consommables alchimistes, equipements elementaires t2/t3 par slot) — ces categories restantes sont desormais le seul axe d'i18n items a couvrir.
  - [x] **3c.m — Fixtures EN pour 6 materias avancees Terre/Vent (tier 2-4)** (2026-04-26) : extension directe de la sous-phase 3c.l (Lumiere/Ombre) qui couvre 3 materias Earth (`materia_boulder_throw=Boulder Throw` m3, `materia_stone_spikes=Stone Spikes` m3, `materia_mountain_strength=Mountain Strength` m3) + 3 materias Air (`materia_tornado=Tornado` m4, `materia_wind_shield=Wind Shield` m3, `materia_air_dash=Air Dash` m2). Couvre les materias avancees offensives + defensives + utilitaires des domaines soldier (Earth) et druid (Air). +6 lignes ajoutees a `ItemFixtures.php`. Aucune migration, aucun nouveau test. Impact EN immediat sur `/game/inventory`. 53 items restent sans traduction.
  - [x] **3c.l — Fixtures EN pour 6 materias avancees Lumiere/Ombre (tier 3-4)** (2026-04-26) : extension directe de la sous-phase 3c.k qui couvre 2 materias avancees Lumiere (`materia_celestial_blessing=Celestial Blessing` m4 — healer, `materia_divine_intervention=Divine Intervention` m4) + 4 materias avancees Ombre (`materia_dark_harvest=Dark Harvest` m4 — necromancer, `materia_soul_rip=Soul Rip` m4, `materia_death_nova=Death Nova` m3, `materia_shadow_mend=Shadow Mend` m3 — soin sombre). Couvre les materias debloquees a haut niveau dans les domaines healer (avancees) et necromancer (avancees). +6 lignes ajoutees a `ItemFixtures.php`. Aucune migration, aucun nouveau test. Impact EN immediat sur `/game/inventory`. 59 items restent sans traduction (consommables alchimistes, materia tier 4+ Earth/Air/Beast/Metal, equipements elementaires t2/t3 par slot).
  - [x] **3c.k — Fixtures EN pour 5 materias avancees Feu+Vie (tier 3-5)** (2026-04-26) : extension directe de la sous-phase 3c.j (Terre/Vent) qui couvre 3 materias avancees Feu (`materia_dragon_breath=Dragon Breath` m4 — pyromancy, `materia_volcanic_eruption=Volcanic Eruption` m5 — volcan ultime, `materia_ember_shield=Ember Shield` m3 — bouclier defensif feu) + 2 materias avancees Vie (`materia_vitality_surge=Vitality Surge` m3 — afflux HP/MP, `materia_life_shield=Life Shield` m3 — bouclier sacre). Couvre les materias debloquees a haut niveau dans les domaines pyromancy (offensif) et healer (defensif/buff). +5 lignes ajoutees a `ItemFixtures.php`. Aucune migration, aucun nouveau test. Impact EN immediat sur `/game/inventory` (materias debloquees a haut niveau), `/game/shop` si certaines de ces materias sont vendues. 65 items restent sans traduction (consommables alchimistes, materia tier 4+ celestial/dark advanced, equipements elementaires t2/t3 par slot).
  - [x] **3c.j — Fixtures EN pour les 5 materias Terre/Vent (domaines soldier + druid)** (2026-04-26) : extension directe de la sous-phase 3c.i (Dark) qui couvre 2 materias Earth (`materia_earthquake=Earthquake`, `materia_rock_armor=Rock Armor` — domaine soldier) + 3 materias Air (`materia_wind_lame=Wind Blade`, `materia_gust=Gust`, `materia_cyclone=Cyclone` — domaine druid). Note : `materia_wind_lame` est definie deux fois dans `ItemFixtures` (lignes 237 et 2054) — la 1ere etait deja traduite, la 2eme (qui ecrase effectivement la 1ere via la convention PHP de cle dupliquee) est traduite ici pour preserver la coherence. +5 lignes ajoutees a `ItemFixtures.php`. Aucune migration, aucun nouveau test. Impact EN immediat sur `/game/inventory` (materias debloquees apres avoir avance dans les domaines soldier/druid), `/game/shop` si certaines de ces materias sont vendues. 70 items restent sans traduction (consommables alchimistes, materia tier 4+ avancees feu/lumiere/ombre, equipements elementaires t2/t3 par slot).
  - [x] **3c.i — Fixtures EN pour les 5 materias Ombre/Mort (domaine necromancer)** (2026-04-26) : extension directe de la sous-phase 3c.h (Lumiere) qui couvre 5 materias avancees du domaine necromancer/element Dark : `materia_soul_drain=Soul Drain` (m2), `materia_death_touch=Death Touch` (m2), `materia_shadow_bolt=Shadow Bolt` (m3), `materia_life_leech=Life Leech` (m2), `materia_shadow_wave=Shadow Wave` (m3). Couvre l'arsenal de necromancie (drain HP/MP, attaque AOE shadow, leech HP). +5 lignes ajoutees a `ItemFixtures.php`. Aucune migration, aucun nouveau test. Impact EN immediat sur `/game/inventory` (materias debloquees apres avoir avance dans le domaine necromancer), `/game/shop` si certaines de ces materias sont vendues, et tout usage en combat. 75 items restent sans traduction (consommables alchimistes, materia tier 3+ pour les autres elements eau/air/terre/metal/bete, equipements elementaires t2/t3 par slot).
  - [x] **3c.h — Fixtures EN pour les 5 materias Lumiere/Vie (domaine healer)** (2026-04-26) : extension directe de la sous-phase 3c.g (Feu) qui couvre 5 materias avancees du domaine healer/element Lumiere : `materia_rejuvenation=Rejuvenation` (m2), `materia_divine_blessing=Divine Blessing` (m3), `materia_healing_wave=Healing Wave` (m2), `materia_life_transfer=Life Transfer` (m2), `materia_holy_light=Holy Light` (m3). Couvre l'arsenal de soin avance (regen progressive, benediction massive, vague AOE, transfert HP entre allies, lumiere divine offensive+curative). +5 lignes ajoutees a `ItemFixtures.php`. Aucune migration, aucun nouveau test. Impact EN immediat sur `/game/inventory` (materias debloquees apres avoir avance dans le domaine healer), `/game/shop` si certaines de ces materias sont vendues, et tout usage en combat. 80 items restent sans traduction (consommables alchimistes, materia tier 3+ pour les autres elements eau/air/terre/metal/bete/ombre, equipements elementaires t2/t3 par slot).
  - [x] **3c.g — Fixtures EN pour les recompenses Acte 3 + materia tier 3-4 element Feu (7 items)** (2026-04-26) : extension directe des sous-phases 3c, 3c.b/c/d/e/f via la meme convention `name_translations` dans `ItemFixtures`. Recompenses Acte 3 (rarete Amethyste) : `convergence_blade=Convergence Blade` (epee main_weapon, slot 3 materia, niveau 25), `convergence_amulet=Convergence Amulet` (collier, slot 2 materia, niveau 25). Materia tier 3-4 element Feu (5 items) : `materia_inferno=Inferno`, `materia_fire_wall=Fire Wall`, `materia_fire_nova=Fire Nova`, `materia_phoenix_flame=Phoenix Flame`, `materia_meteor_strike=Meteor Strike`. +7 lignes ajoutees a `ItemFixtures.php`. Aucune migration, aucun nouveau test. Impact EN immediat sur `/game/inventory` (table de loot du Gardien de la Convergence + materias debloquees apres avoir avance dans le domaine pyromancy), `/game/shop` si certains de ces items sont vendus, et tout dialogue PNJ qui mentionne ces recompenses. 85 items restent sans traduction (consommables alchimistes avances, materia tier 3+ pour les autres elements eau/air/terre/metal/bete/lumiere/ombre, equipements elementaires t2/t3 par slot) — couverts dans les sous-phases ulterieures.
  - [x] **3c.f — Fixtures EN pour les noms des 6 quest items (chaine narrative Acte 1-2)** (2026-04-26) : extension directe des sous-phases 3c, 3c.b, 3c.c, 3c.d, 3c.e via la meme convention `name_translations` dans `ItemFixtures`. Items couverts : `quest_item_ancient_key=Ancient Key`, `quest_item_magic_gem=Magic Gem`, `quest_item_fragment_foret=Sylvan Fragment`, `quest_item_fragment_mines=Forge Fragment`, `quest_item_fragment_marais=Mist Fragment`, `quest_item_fragment_montagne=Summit Fragment`. +6 lignes ajoutees a `ItemFixtures.php`. Aucune migration, aucun nouveau test : infrastructure (3a) + cablage Twig (3b) deja en place. Impact EN immediat sur l'inventaire (`/game/inventory`), l'ecran de quetes (`/game/quest`) et tout dialogue PNJ qui mentionne ces items via `{{ item|localized_name }}`. Zero impact FR (fallback transparent preserve). 92 items restent sans traduction (consommables alchimistes avances, recompenses Acte 3 `convergence_*`, materia tier 4+, equipements elementaires t2/t3 par slot) — couverts dans les sous-phases ulterieures.
  - [x] **3c.e — Fixtures EN pour les noms des armes generiques tier 1-3 (15 items)** (2026-04-24) : extension directe des sous-phases 3c (35 items de debut de jeu), 3c.b (26 materia tier 1-3), 3c.c (18 materiaux de craft) et 3c.d (27 plantes/herbes) aux 15 armes generiques non elementaires (axe/bow/dagger/lance/staff x 3 tiers) via la meme convention `name_translations` dans `ItemFixtures`. Tier 1 (5) : `t1-axe=Rusty Hatchet`, `t1-bow=Short Bow`, `t1-dagger=Chipped Dagger`, `t1-lance=Wooden Pike`, `t1-staff=Novice Staff`. Tier 2 (5) : `t2-axe=War Axe`, `t2-bow=Composite Longbow`, `t2-dagger=Mithril Dagger`, `t2-lance=Steel Lance`, `t2-staff=Crystal Staff`. Tier 3 epic (5) : `t3-axe=Berserker Axe`, `t3-bow=Howling Wind Bow`, `t3-dagger=Shadow Blade`, `t3-lance=Celestial Knight Lance`, `t3-staff=Archmage Staff`. +15 lignes ajoutees a `ItemFixtures.php` (4213 au total, respecte la limite de 50 lignes ajoutees a un fichier >400 lignes). Aucune migration, aucun nouveau test : infrastructure (3a) et cablage Twig (3b) deja en place, `ItemLocalizationTest` couvre deja la mecanique. Impact EN immediat sur `/game/shop` (armes vendues par les forgerons/PNJ) et `/game/inventory` (onglets equipement + selecteurs de materia) via le filter `localized_name`. Zero impact FR (fallback transparent preserve). Les items restants (equipements elementaires t2/t3 par slot, consommables alchimistes avances, recettes craft, fragments Acte 2+, recompenses boss legendaires) conservent leur nom FR comme fallback (couvert dans une sous-phase ulterieure).
  - [x] **3c.d — Fixtures EN pour les noms des plantes et herbes (27 items)** (2026-04-24) : extension directe des sous-phases 3c (35 items de debut de jeu), 3c.b (26 materia tier 1-3) et 3c.c (18 materiaux de craft) a 27 plantes/herbes via la meme convention `name_translations` dans `ItemFixtures`. Plantes communes (6) : `plant-lavender=Lavender`, `plant-mint=Mint`, `plant-sage=Sage`, `plant-thyme=Thyme`, `plant-rosemary=Rosemary`, `plant-chamomile=Chamomile`. Plantes medicinales (6) : `plant-nettle=Nettle`, `plant-dandelion=Dandelion`, `plant-valerian=Valerian`, `plant-aloe-vera=Aloe Vera`, `plant-ginseng=Ginseng`, `plant-echinacea=Echinacea`. Plantes toxiques (3) : `plant-mandrake=Mandrake`, `plant-nightshade=Nightshade`, `plant-wolfsbane=Wolfsbane`. Plantes magiques (8) : `plant-moonflower=Moonflower`, `plant-sunblossom=Sunblossom`, `plant-dragonleaf=Dragonleaf`, `plant-frostcap=Frostcap`, `plant-ghostshroom=Ghostshroom`, `plant-thunderroot=Thunderroot`, `plant-whisperweed=Whisperweed`, `plant-dreamlily=Dreamlily`. Plantes legendaires (2) : `plant-voidfruit=Voidfruit`, `plant-phoenixflower=Phoenix Flower`. Ingredients marais / champignons (2, slugs sans prefixe `plant-`) : `poisonous-mushroom=Poisonous Mushroom`, `swamp-root=Swamp Root`. +27 lignes ajoutees a `ItemFixtures.php` (4198 au total, respecte la limite de 50 lignes ajoutees a un fichier >400 lignes). Aucune migration, aucun nouveau test : infrastructure (3a) et cablage Twig (3b) deja en place, `ItemLocalizationTest` couvre deja la mecanique. Impact EN immediat sur `/game/inventory` (onglet items), `/game/shop` si une plante est exposee, et les vues de craft/alchimie via le filter `localized_name`. Zero impact FR (fallback transparent preserve). Les items restants (equipements tier 2+, consommables alchimiste avances, recettes craft, fragments Acte 2+) conservent leur nom FR comme fallback (couvert dans une sous-phase ulterieure).
  - [x] **3c.c — Fixtures EN pour les noms des materiaux de craft (ores + lingots + gemmes)** (2026-04-24) : extension directe des sous-phases 3c (35 items de debut de jeu) et 3c.b (26 materia tier 1-3) a 18 materiaux d'artisanat via la meme convention `name_translations` dans `ItemFixtures`. Minerais precieux (3) : `ore-ruby=Ruby`, `ore-diamond=Diamond`, `ore-emerald=Emerald`. Lingots metalliques (7) : `crafted-bronze-ingot=Bronze Ingot`, `crafted-iron-ingot=Iron Ingot`, `crafted-cobalt-ingot=Cobalt Ingot`, `crafted-mithril-ingot=Mithril Ingot`, `crafted-adamantite-ingot=Adamantite Ingot`, `crafted-orichalcum-ingot=Orichalcum Ingot`, `crafted-gold-ingot=Gold Ingot`. Materiaux tannerie / tisserand / alchimie (3) : `crafted-leather-strip=Leather Strip`, `crafted-cloth=Cloth`, `crafted-potion-base=Potion Base`. Gemmes taillees joaillier (5) : `crafted-gem-basic=Cut Gem`, `crafted-gem-fine=Fine Gem`, `crafted-gem-rare=Rare Gem`, `crafted-gem-enchanted=Enchanted Gem`, `crafted-gem-prismatic=Prismatic Gem`. +18 lignes ajoutees a `ItemFixtures.php` (4171 au total, respecte la limite de 50 lignes ajoutees a un fichier >400 lignes). Aucune migration, aucun nouveau test : infrastructure (3a) et cablage Twig (3b) deja en place, `ItemLocalizationTest` couvre deja la mecanique. Impact EN immediat sur `/game/shop`, `/game/inventory`, `/game/bestiary` (table de loot) et les selecteurs de materia via le filter `localized_name`. Zero impact FR (fallback transparent preserve). Les items restants (equipements tier 2+, consommables alchimiste avances, herbes / plantes, recettes craft, fragments Acte 2+) conservent leur nom FR comme fallback (couvert dans une sous-phase ulterieure).
  - [x] **3c.b — Fixtures EN pour les noms des materia tier 1-3** (2026-04-23) : extension de la sous-phase 3c aux 26 materia existantes via la meme convention `name_translations` dans `ItemFixtures`. Tier 1 (10) : m1-life=Heal, m1-fire=Fireball, m1-flame=Flame, m2-fire=Fire (slug legacy sur entree tier 1 bas cout), m3-fire=Rain of Fire, m1-earth=Stone Throw, m1-death=Punishment, m1-wind=Wind Blade, m1-metal=Sharp Blade, m1-nature=Vine Whip. Tier 2 (8, 1 par element) : m2-combustion=Combustion, m2-water=Frost Mist, m2-air=Chain Lightning, m2-earth=Stone Wall, m2-metal=Steel Riposte, m2-beast=Savage Bite, m2-light=Blessing, m2-dark=Vital Drain. Tier 3 (8 epiques, 1 par element) : m3-fire-solar=Solar Burst, m3-water=Frost Maelstrom, m3-air=Thunderstorm, m3-earth=Crystal Quake, m3-metal=Orichalcum Blade, m3-beast=Primal Awakening, m3-light=Divine Grace, m3-dark=Shadow Covenant. Aucune migration, aucun nouveau test : infrastructure (3a) et cablage Twig (3b) deja en place, `ItemLocalizationTest` couvre deja la mecanique. Impact EN immediat sur les selecteurs de materia (`inventory/materia/_slot_select*`), l'inventaire (onglet materia) et `/game/shop` si une boutique expose une materia. Zero impact FR (fallback transparent preserve). Les items restants (equipements tier 2+, gemmes, recettes craft, fragments Acte 2+) conservent leur nom FR comme fallback (couvert dans une sous-phase ulterieure).
  - [ ] **3d** — extension a `Item.description` (meme pattern, colonne `description_translations`)
  - [~] **3e** — extension aux entites `Spell`, `Quest`, `Monster`, `Pnj`, `Race` (noms + descriptions/dialogues)
      - [x] **3e.p — Infrastructure + cablage Twig + fixtures EN pour les titres et descriptions de defis hebdomadaires de guilde** (2026-04-28) : colonnes JSON `title_translations` et `description_translations` ajoutees sur `weekly_challenge` (migration `Version20260428WeeklyChallengeTranslations`, idempotente via `ADD COLUMN IF NOT EXISTS`). Entite `WeeklyChallenge` etendue avec `getLocalizedTitle(?string $locale): string` + `getLocalizedDescription(?string $locale): string` (les deux retournent `string` non-nullable car les colonnes de base le sont, comme Faction en 3e.h ou Region en 3e.g) + setters normalises (cles vides / valeurs non-string / valeurs blanches filtrees, compaction vers `null` si aucune entree valide). Nouvelle extension Twig `WeeklyChallengeLocalizationExtension` (filters `localized_challenge_title` + `localized_challenge_description`, noms distincts de `localized_quest_*` / `localized_faction_*` pour eviter la collision Twig). Template `templates/game/guild/challenges.html.twig` cable sur 3 occurrences (defis actifs : titre + description ; defis termines : titre seul). `WeeklyChallengeFixtures` propage `setTitleTranslations(['en' => ...])` + `setDescriptionTranslations(['en' => ...])` pour les 4 defis (Chasseur infatigable=Tireless Hunter / Forge ardente=Burning Forge / Moisson abondante=Bountiful Harvest / Heros du peuple=Heroes of the People). Tests unitaires `WeeklyChallengeLocalizationTest` (14 cas : 7 sur title + 7 sur description, fallback / match locale EN-DE / fallback locale missing / ignore blank/invalid / reset null / reset only-invalid / default empty array). Aucune autre route (REST/SSE) n'expose les defis : la traduction se fait uniquement cote rendu Twig. Atteint **100% de parite FR/EN sur les 4 defis hebdomadaires** en une seule sous-phase. Independante des 15 PR i18n / avatar / mounts / events en vol.
    - [x] **3e.f — Infrastructure multilingue pour les noms et descriptions de races** (2026-04-25) : colonnes JSON `name_translations` et `description_translations` ajoutees sur `game_races` (migration `Version20260425RaceTranslations`, idempotente via `ADD COLUMN IF NOT EXISTS` / `DROP COLUMN IF EXISTS`). Entite `Race` etendue avec `getLocalizedName(?string $locale): string` et `getLocalizedDescription(?string $locale): string` (fallback gracieux sur `$this->name` / `$this->description` si locale nulle/vide, colonne nulle, locale absente ou valeur blanche/non-string), `getNameTranslations(): array` + `getDescriptionTranslations(): array` (defaut `[]`) et `setNameTranslations(?array $translations): self` + `setDescriptionTranslations(?array $translations): self` avec normalisation (cles vides filtrees, valeurs non-string ignorees via `is_string`, valeurs blanches filtrees via `trim`, compaction vers `null` si aucune entree valide). Miroir strict des sous-phases 3a (Item), 3e.a (Monster), 3e.c.d.quest + 3e.c.d.quest.d (Quest), 3e.c.domain (Domain) et 3e.c.achievement (Achievement). Tests `RaceLocalizationTest` (14 cas : 7 pour `name` + 7 pour `description`). Aucun template (`templates/game/character/select.html.twig`, `templates/game/character/create.html.twig`, `templates/game/profile/show.html.twig`) ni controller modifie : les sous-phases de cablage (filter Twig `localized_race_name`) et de fixtures EN (4 races : Humain, Elfe, Nain, Orc) suivront. Sous-phase independante des 13 PR i18n / avatar / mounts / events en vol : aucune ne touche `Race.php` ni la table `game_races`.
    - [x] **3e.f.b — Cablage des filters `localized_race_name` + `localized_race_description`** (2026-04-25) : nouvelle extension Twig `App\Twig\RaceLocalizationExtension` qui expose 2 filters (`localized_race_name`, `localized_race_description`) appliquant respectivement `Race::getLocalizedName` et `Race::getLocalizedDescription` avec la locale courante recuperee depuis `RequestStack` (fallback transparent sur `Race::name` / `Race::description` si RequestStack vide, Race null ou traduction manquante). Noms distincts de `localized_name` (Item), `localized_monster_name` (Monster), `localized_quest_name` / `localized_quest_description` (Quest), `localized_domain_title` (Domain) et `localized_achievement_title` / `localized_achievement_description` (Achievement) pour eviter la collision Twig entre filters typehintes sur entites differentes. Templates cables : `templates/game/character/select.html.twig` (2 occurrences : `alt` du sprite et libelle de la race sous le nom du personnage), `templates/game/character/create.html.twig` (3 occurrences : `alt` du sprite, en-tete `<h3>` et description `<p>`) et `templates/game/profile/show.html.twig` (1 occurrence : libelle de race sur le profil public). Tests `RaceLocalizationExtensionTest` (9 cas : 4 pour `localized_race_name`, 4 pour `localized_race_description`, 1 pour l'enregistrement des 2 filters). Une fois la sous-phase 3e.f.c (fixtures EN pour les 4 races) livree, chaque paire `{fr, en}` sera automatiquement affichee dans les 3 templates cables sans redeploiement.
    - [x] **3e.f.c — Fixtures EN pour les noms et descriptions des 4 races** (2026-04-25) : `RaceFixtures` cable directement les traductions EN sur les 4 races standards via `setNameTranslations(['en' => ...])` et `setDescriptionTranslations(['en' => ...])` apres les setters FR existants : `human=Human`, `elf=Elf`, `dwarf=Dwarf`, `orc=Orc` cote nom + descriptions complementaires (constitution / agilite / robustesse / force brute). Aucune migration necessaire : les colonnes JSON `name_translations` + `description_translations` sont deja creees par la sous-phase 3e.f. Aucun nouveau test : `RaceLocalizationTest` (14 cas) couvre deja la mecanique d'I/O et `RaceLocalizationExtensionTest` (9 cas) couvre le cablage Twig. Zero impact FR (le fallback transparent de `Race::getLocalizedName` / `getLocalizedDescription` preserve le rendu actuel) ; impact EN immediat sur `templates/game/character/select.html.twig`, `templates/game/character/create.html.twig` et `templates/game/profile/show.html.twig` via les filters `localized_race_name` / `localized_race_description` cables en 3e.f.b. Atteint **100% de parite FR/EN sur les 4 races du jeu**.
    - [x] **3e.g — Infrastructure + cablage + fixtures EN pour les noms et descriptions de regions** (2026-04-25) : colonnes JSON `name_translations` et `description_translations` ajoutees sur `region` (migration `Version20260425RegionTranslations`, idempotente via `ADD COLUMN IF NOT EXISTS` / `DROP COLUMN IF EXISTS`). Entite `App\Entity\App\Region` etendue avec `getLocalizedName(?string $locale): string` et `getLocalizedDescription(?string $locale): ?string` (signature ?string car `Region::description` est nullable, contrairement a Race), `getNameTranslations` + `setNameTranslations`, `getDescriptionTranslations` + `setDescriptionTranslations`, normalisation identique au pattern Race / Achievement (cles vides filtrees, valeurs non-string ignorees via `is_string`, valeurs blanches filtrees via `trim`, compaction vers `null`). Tests `RegionLocalizationTest` (15 cas : 7 pour `name`, 7 pour `description` + 1 cas additionnel verifiant que `getLocalizedDescription` retourne `null` quand la description de base est null). Nouvelle extension Twig `App\Twig\RegionLocalizationExtension` exposant 2 filters (`localized_region_name`, `localized_region_description`) qui appellent `RequestStack::getCurrentRequest()->getLocale()` puis delegue a l'entite (fallback transparent sur RequestStack vide / Region null / traduction manquante / description null retournant `''`). Tests `RegionLocalizationExtensionTest` (9 cas). Templates cables avec les filters : `templates/game/gold_sink/index.html.twig` (liste des destinations de teleportation rapide), `templates/game/guild/upgrades.html.twig` (cards regions de l'admin guilde) et `templates/game/guild/influence.html.twig` (selecteur de region). `RegionFixtures` recoit les traductions EN sur les 3 regions standards via `setNameTranslations(['en' => ...])` et `setDescriptionTranslations(['en' => ...])` apres les setters FR : `Plaines de l'Eveil=Plains of Awakening`, `Sanctuaire de Lumiere=Sanctuary of Light`, `Terres Sauvages=Wildlands` cote nom + descriptions complementaires. Atteint **100% de parite FR/EN sur les 3 regions du jeu** en une seule sous-phase (infra + cablage + fixtures), pattern eprouve par 3e.f Race. Sous-phase independante des 13+ PR i18n / avatar / mounts / events en vol : aucune ne touche `Region.php` ni la table `region`, zero risque de conflit.
    - [x] **3e.a — Infrastructure multilingue pour les noms de monstres** (2026-04-21) : colonne JSON `name_translations` ajoutee sur `game_monsters` (migration `Version20260421MonsterNameTranslations`, idempotente via `ADD COLUMN IF NOT EXISTS` / `DROP COLUMN IF EXISTS`). Entite `Monster` etendue avec `getLocalizedName(?string $locale): string` (fallback gracieux sur `$this->name` si locale nulle/vide, colonne nulle, locale absente ou valeur blanche/non-string), `getNameTranslations(): array` (defaut `[]`) et `setNameTranslations(?array $translations): self` avec normalisation (cles/valeurs vides filtrees, valeurs non-string ignorees, stockage compact en `null` si aucune entree valide). Miroir strict de la sous-phase 3a (Item). Tests `MonsterLocalizationTest` (7 cas). Aucun template ni controller modifie : les sous-phases 3e.b (cablage) et 3e.c (fixtures EN) suivront.
    - [~] **3e.b** — cablage dans les templates (bestiaire `name` d'entete + templates combat / loot) + fixtures EN
      - [x] **3e.b.a — Cablage du filter `localized_monster_name`** (2026-04-21) : nouveau filter Twig `localized_monster_name` (extension `App\Twig\MonsterLocalizationExtension`) qui applique `Monster::getLocalizedName` avec la locale courante recuperee depuis `RequestStack` (fallback transparent sur `Monster::name` si RequestStack vide, Monster null ou traduction manquante). Nom distinct de `localized_name` (Item) pour eviter la collision Twig entre filters typehintes sur entites differentes. Templates cables avec `{{ monster|localized_monster_name }}` : `templates/game/bestiary/index.html.twig` (3 occurrences : en-tete carte monstre + 2 `|trans({'%monster%': ...})` sur `game.bestiary.tier_title`), `templates/game/profile/show.html.twig` (liste des titres de chasseur), `templates/game/fight/index.html.twig` (bouton depecage `game.fight.butcher` avec le nom du monstre). Le template `templates/game/quest/index.html.twig` n'est pas cable : `tracking.monsters` y est un tableau associatif (non une entite `Monster`) - cablage deporte en 3e.b.c. Tests `MonsterLocalizationExtensionTest` (5 cas : traduction matchee, fallback base name, RequestStack vide, Monster null, enregistrement du filter). Une fois la sous-phase 3e.b.b (fixtures EN pour les noms de monstres) livree, chaque paire `{fr, en}` sera automatiquement affichee dans les 3 templates cables sans redeploiement de template.
      - [x] **3e.b.b — Fixtures EN pour les noms des monstres de debut de jeu** (2026-04-22) : `MonsterFixtures` accepte desormais la cle optionnelle `name_translations` (array) dans chaque entree du tableau de monstres, qui est passee a `Monster::setNameTranslations()` lors de la construction. 24 monstres de niveaux 1-3 recoivent une traduction EN : niveau 1 (slime=Slime, goblin=Goblin, bat=Bat, giant_rat=Giant Rat, zombie=Zombie, wolf=Wolf, beetle=Beetle), niveau 2 (skeleton=Skeleton, spider=Spider, venom_snake=Venomous Snake, taiju=Taiju, specter=Specter, banshee=Banshee, scorpion=Scorpion, mushroom_golem=Mushroom Golem, ghost=Ghost, undine=Undine), niveau 3 (ochu=Ochu, werewolf=Werewolf, gargoyle=Gargoyle, troll=Troll, fire_elemental=Fire Elemental, salamander=Salamander, rusty_automaton=Rusty Automaton). Les monstres de niveau 4+ et les boss gardent leur nom FR comme fallback (couvert dans une sous-phase ulterieure). Aucune migration necessaire : la colonne JSON `name_translations` est deja creee par la sous-phase 3e.a. Zero impact FR (le fallback transparent de `Monster::getLocalizedName` preserve le rendu actuel) ; impact EN immediat sur `/game/bestiary`, `/game/profile`, ecran de combat et bouton de depecage (via le filter `localized_monster_name` cable en 3e.b.a).
      - [x] **3e.b.b.suite — Fixtures EN pour le reste du bestiaire (niveaux 3+ et boss)** (2026-04-22) : complete la sous-phase 3e.b.b en couvrant les 22 monstres restants (niveaux 3 a 30, boss d'Acte inclus), amenant le bestiaire a 100% de parite FR/EN sur les noms affiches. Niveau 3 : `necromancer=Necromancer`, `will_o_wisp=Will-o'-the-Wisp`. Niveau 4 : `alpha_wolf=Alpha Wolf`, `griffin=Griffin`, `minotaur=Minotaur`, `stone_golem=Stone Golem`, `sylph=Sylph`. Niveau 5 : `clay_golem=Clay Golem`, `creeping_shadow=Creeping Shadow`, `dragon=Ancestral Dragon`. Niveaux 10-18 : `wyvern=Wyvern`, `cursed_knight=Cursed Knight`, `naga=Naga`, `crystal_golem=Crystal Golem`, `forest_guardian=Forest Guardian`, `corrupted_archdruid=Corrupted Archdruid`, `lesser_lich=Lesser Lich`. Boss Acte 2-3 (niveaux 20-30) : `forge_lord=Forge Lord`, `swamp_hydra=Swamp Hydra`, `abyssal_blacksmith=Abyssal Blacksmith`, `ancient_wyrm=Ancient Wyrm`, `convergence_guardian=Guardian of the Convergence`. Aucune migration necessaire. Zero impact FR (fallback transparent preserve) ; impact EN immediat et complet sur `/game/bestiary`, `/game/profile` (titres de chasseur), ecran de combat et bouton de depecage.
      - [x] **3e.b.b.fix — Fixture EN pour le boss `Racine Ancienne`** (2026-04-26) : completion d'un gap residuel apres la sous-phase 3e.b.b.suite (qui annoncait 100% de parite mais avait omis le boss `ancient_root` du donjon "Racines de la foret", tache 84). Ajout de `'name_translations' => ['en' => 'Ancient Root']` dans `MonsterFixtures` (1 ligne). Verification systematique via `awk` : seul ce monstre etait sans `name_translations` cote `MonsterFixtures::$monsters` racine (les autres entrees `'name' =>` sans `name_translations` sont des elements imbriques `bossPhases` qui ne sont pas exposees comme noms de monstres a l'utilisateur). Atteint reellement 100% de parite FR/EN sur les noms affiches dans le bestiaire / combat / profile / depecage. Aucune migration. Aucun nouveau test. Diff : +1 ligne fixture.
      - [ ] **3e.b.c** — cablage du tracking de quetes (`templates/game/quest/index.html.twig`) : la source `tracking.monsters` etant un tableau associatif assemble cote controller, le cablage passera par l'injection de la locale dans la construction du tableau, meme pattern que `Inventory\ItemsController` en sous-phase 3b.
    - [~] **3e.c** — meme pattern pour `Spell.name`, `Quest.name`, `Pnj.name`
      - [x] **3e.c.d.quest — Infrastructure multilingue pour les noms de quetes** (2026-04-22) : colonne JSON `name_translations` ajoutee sur `game_quests` (migration `Version20260422QuestNameTranslations`, idempotente via `ADD COLUMN IF NOT EXISTS` / `DROP COLUMN IF EXISTS`). Entite `Quest` etendue avec `getLocalizedName(?string $locale): string` (fallback gracieux sur `$this->name` si locale nulle/vide, colonne nulle, locale absente ou valeur blanche/non-string), `getNameTranslations(): array` (defaut `[]`) et `setNameTranslations(?array $translations): self` avec normalisation (cles vides filtrees, valeurs non-string ignorees via `is_string`, valeurs blanches filtrees via `trim`, compaction vers `null` si aucune entree valide). Miroir strict des sous-phases 3a (Item), 3e.a (Monster), 3e.c.a (Spell) et 3e.d (Pnj). Tests `QuestLocalizationTest` (7 cas). Aucun template ni controller modifie : le cablage (journal de quetes + dialogues PNJ + HUD tracking) sera livre en sous-phase dediee.
      - [x] **3e.c.d.quest.b — Cablage du filter `localized_quest_name`** (2026-04-22) : nouveau filter Twig `localized_quest_name` (extension `App\Twig\QuestLocalizationExtension`) qui applique `Quest::getLocalizedName` avec la locale courante recuperee depuis `RequestStack` (fallback transparent sur `Quest::name` si RequestStack vide, Quest null ou traduction manquante). Nom distinct de `localized_name` (Item) et `localized_monster_name` (Monster) pour eviter la collision Twig entre filters typehintes sur entites differentes. Templates cables avec `{{ quest|localized_quest_name }}` : `templates/game/quest/index.html.twig` (6 occurrences : en-tetes de quetes en cours, quotidiennes actives, quotidiennes terminees, quetes disponibles, quotidiennes disponibles et quetes completees dans l'historique) et `templates/game/index.html.twig` (tracking des quetes actives sur le dashboard `/game`). Les templates `templates/admin/**` et `templates/old_game/**` ne sont pas cables (hors scope jeu). Tests `QuestLocalizationExtensionTest` (5 cas : traduction matchee, fallback base name, RequestStack vide, Quest null, enregistrement du filter). Une fois la sous-phase 3e.c.d.quest.c (fixtures EN pour les noms de quetes) livree, chaque paire `{fr, en}` sera automatiquement affichee dans les 2 templates cables sans redeploiement de template.
      - [x] **3e.c.d.quest.c — Fixtures EN pour les noms des quetes de debut de jeu** (2026-04-22) : `QuestFixtures` accepte desormais la cle optionnelle `name_translations` (array) dans chaque entree du tableau de quetes, qui est passee a `Quest::setNameTranslations()` lors de la construction (juste apres `setName`). 26 quetes de debut de jeu recoivent une traduction EN : quetes de monstres (`quest_zombie_1`, `quest_skeleton_1`, `quest_taiju_1`, `quest_mushroom_1`, `quest_goblin_1`, `quest_troll_1`, `quest_werewolf_1`, `quest_banshee_griffin_1`), quetes utilitaires (`quest_wood_collection`, `quest_deliver_mushroom`, `quest_explore_forest`, `quest_choice_alliance`), 6 dailies (`daily_kill_slimes`, `daily_kill_bats`, `daily_kill_spiders`, `daily_collect_herbs`, `daily_collect_ore`, `daily_kill_rats`), chaine "La Menace Rampante" (`quest_chain_guard_1..3`) et chaine tutoriel Acte 1 (`quest_acte1_reveil`, `quest_acte1_premiers_pas`, `quest_acte1_bapteme_du_feu`, `quest_acte1_recolte`, `quest_acte1_cristal`). Les quetes restantes (dragons, Acte 2+, chaines cachees, choix d'alignement avances) conservent leur nom FR comme fallback (couvert dans une sous-phase ulterieure). Aucune migration necessaire : la colonne JSON `name_translations` est deja creee par la sous-phase 3e.c.d.quest. Zero impact FR (le fallback transparent de `Quest::getLocalizedName` preserve le rendu actuel) ; impact EN immediat sur `/game/quests` et `/game` dashboard (via le filter `localized_quest_name` cable en 3e.c.d.quest.b).
      - [x] **3e.c.d.quest.d — Infrastructure multilingue pour les descriptions de quetes** (2026-04-23) : colonne JSON `description_translations` ajoutee sur `game_quests` (migration `Version20260423QuestDescriptionTranslations`, idempotente via `ADD COLUMN IF NOT EXISTS` / `DROP COLUMN IF EXISTS`). Entite `Quest` etendue avec `getLocalizedDescription(?string $locale): string` (fallback gracieux sur `$this->description` si locale nulle/vide, colonne nulle, locale absente ou valeur blanche/non-string), `getDescriptionTranslations(): array` (defaut `[]`) et `setDescriptionTranslations(?array $translations): self` avec normalisation (cles vides filtrees, valeurs non-string ignorees via `is_string`, valeurs blanches filtrees via `trim`, compaction vers `null` si aucune entree valide). Miroir strict de la sous-phase 3e.c.d.quest (qui couvrait `name`). Tests `QuestDescriptionLocalizationTest` (7 cas). Aucun template ni controller modifie : le cablage (journal de quetes + dialogues PNJ + HUD tracking) et les fixtures EN des descriptions suivront en sous-phases dediees.
      - [x] **3e.c.d.quest.e — Cablage du filter `localized_quest_description`** (2026-04-23) : `QuestLocalizationExtension` etendue avec un second filter Twig `localized_quest_description` qui applique `Quest::getLocalizedDescription` avec la locale courante recuperee depuis `RequestStack` (fallback transparent sur `Quest::description` si RequestStack vide, Quest null ou traduction manquante). Nom distinct de `localized_quest_name` pour rester coherent avec la convention un-filter-par-champ deja etablie pour les items / monstres. Templates cables avec `{{ quest|localized_quest_description }}` : `templates/game/quest/index.html.twig` (5 occurrences : quetes en cours, quotidiennes actives, quotidiennes terminees, quetes disponibles, historique avec slice) et `templates/game/index.html.twig` (tracking des quetes actives sur le dashboard `/game` avec slice a 80 caracteres). Les patterns avec `|slice(0, 80)` utilisent une variable locale `{% set questDescription = quest|localized_quest_description %}` pour eviter de payer le cout du filter deux fois (rendu + test de longueur). Les templates `templates/admin/**` et `templates/old_game/**` ne sont pas cables (hors scope jeu). Tests `QuestLocalizationExtensionTest` etendus (9 cas : +4 pour le nouveau filter avec les memes assertions que pour `localizedQuestName`, +mise a jour de l'enregistrement a 2 filters). Une fois la sous-phase 3e.c.d.quest.f (fixtures EN pour les descriptions de quetes) livree, chaque paire `{fr, en}` sera automatiquement affichee dans les 2 templates cables sans redeploiement de template.
      - [x] **3e.c.d.quest.f — Fixtures EN pour les descriptions des quetes de debut de jeu** (2026-04-23) : `QuestFixtures` accepte desormais la cle optionnelle `description_translations` (array) dans chaque entree du tableau de quetes, qui est passee a `Quest::setDescriptionTranslations()` lors de la construction (juste apres `setDescription`). 26 quetes de debut de jeu recoivent leur description EN (meme couverture que la sous-phase 3e.c.d.quest.c pour les noms) : quetes de monstres (`quest_zombie_1`, `quest_skeleton_1`, `quest_taiju_1`, `quest_mushroom_1`, `quest_goblin_1`, `quest_troll_1`, `quest_werewolf_1`, `quest_banshee_griffin_1`), quetes utilitaires (`quest_wood_collection`, `quest_deliver_mushroom`, `quest_explore_forest`, `quest_choice_alliance`), 6 dailies (`daily_kill_slimes`, `daily_kill_bats`, `daily_kill_spiders`, `daily_collect_herbs`, `daily_collect_ore`, `daily_kill_rats`), chaine "La Menace Rampante" (`quest_chain_guard_1..3`) et chaine tutoriel Acte 1 (`quest_acte1_reveil`, `quest_acte1_premiers_pas`, `quest_acte1_bapteme_du_feu`, `quest_acte1_recolte`, `quest_acte1_cristal`). Les descriptions des quetes restantes (Acte 2+, chaines cachees, choix d'alignement avances) conservent leur contenu FR comme fallback (couvert dans une sous-phase ulterieure). Aucune migration necessaire : la colonne JSON `description_translations` est deja creee par la sous-phase 3e.c.d.quest.d. Zero impact FR (le fallback transparent de `Quest::getLocalizedDescription` preserve le rendu actuel) ; impact EN immediat sur `/game/quests` et `/game` dashboard (via le filter `localized_quest_description` cable en 3e.c.d.quest.e).
      - [x] **3e.c.d.quest.g — Fixtures EN pour 20 quetes supplementaires (Acte 1 secondaires + chaine Acte 2 "Les Fragments")** (2026-04-23) : extension des sous-phases 3e.c.d.quest.c (noms) et 3e.c.d.quest.f (descriptions) au deuxieme lot de quetes. Acte 1 secondaires (5) : `quest_dragon_1=The Mountain Dragon`, `quest_hidden_secret_clearing=The Clearing's Secret`, `quest_hidden_rare_slime=The Golden Slime`, `quest_hidden_herb_lore=Ancestral Lore`, `quest_hidden_goblin_cache=The Goblin Hideout`. Chaine Acte 2 "Les Fragments" — Forêt (4) : `foret_murmures=The Whispers Grow Stronger`, `foret_purification=Purify the Corruption`, `foret_remede=The Ancestral Remedy`, `foret_fragment=The Sylvan Shard`. Mines (4) : `mines_tremblements=Underground Tremors`, `mines_minerai=The Ancient Ore`, `mines_forge=The Forge Lord`, `mines_fragment=The Forge Shard`. Marais (4) : `marais_brumes=The Mists Thicken`, `marais_ingredients=Remedies of the Depths`, `marais_gardiens=Guardians of the Dead Waters`, `marais_fragment=The Mist Shard`. Montagne (3) : `montagne_echos=Echoes from the Summit`, `montagne_gardien=The Peak Guardian`, `montagne_fragment=The Summit Shard`. +40 lignes ajoutees a `QuestFixtures.php` (1976 au total, respecte la limite de 50 lignes ajoutees a un fichier >400 lignes). Aucune migration, aucun template, aucun controller touche. Les quetes restantes (Acte 3 La Convergence, quetes de guilde, quetes karma, quetes cachees steles/cartographe) conservent leur contenu FR comme fallback ; leurs traductions suivront dans des sous-phases ulterieures.
      - [x] **3e.c.d.quest.h — Fixtures EN pour 15 quetes supplementaires (Acte 3 La Convergence + dailies + steles de decouverte)** (2026-04-23) : extension directe de la sous-phase 3e.c.d.quest.g, couvre l'integralite de la chaine narrative finale Acte 3 (3 quetes) + 2 quetes Acte 2 cleanup + 2 dailies festival + 8 quetes cachees de decouverte (exploration standard par zone). Acte 2 cleanup (2) : `quest_enquete_herboriste=The Missing Herbalist`, `quest_defi_gardien_foret=Guardian's Challenge`. Chaine Acte 3 "La Convergence" (3) : `quest_acte3_appel=The Call of the Fragments`, `quest_acte3_gardien=Guardian of the Nexus`, `quest_acte3_epilogue=Epilogue`. Quetes d'evenement (2) : `quest_event_lunar_hunt=Hunt Under the Moon`, `quest_event_shadow_purge=Shadow Purge`. Quetes cachees de decouverte (8) : `quest_discovery_ancient_stele=Forgotten Stele`, `quest_discovery_forgotten_well=The Well of the Ancients`, `quest_discovery_fairy_ring=Fairy Ring`, `quest_discovery_hollow_oak=The Ancient Oak`, `quest_discovery_sunken_altar=Sunken Altar`, `quest_discovery_phospho_grotto=Phosphorescent Grotto`, `quest_discovery_wind_shrine=Wind Shrine`, `quest_discovery_shadow_obelisk=Shadow Obelisk`. +30 lignes ajoutees a `QuestFixtures.php` (2006 au total, respecte la limite de 50 lignes ajoutees a un fichier >400 lignes). Aucune migration, aucun template, aucun controller touche. Les quetes restantes (~25 quetes : cartographe/pelerinage, quetes de guilde, quetes karma, Acte 2 fragments karma) conservent leur contenu FR comme fallback ; leurs traductions suivront dans des sous-phases ulterieures.
      - [x] **3e.c.d.quest.i — Fixtures EN pour 24 quetes supplementaires (decouverte multi-points + zones secondaires + factions + chasse + choix moraux + defense/escorte + sphinx)** (2026-04-23) : extension directe des sous-phases 3e.c.d.quest.g/h, applique le meme pattern (`name_translations` + `description_translations`) a 24 quetes restantes pour atteindre 99% de couverture EN sur les fixtures de quetes. Quetes de decouverte multi-points (2) : `quest_discovery_cartographer=Cartographer of the Forgotten Lands`, `quest_discovery_sacred_sites=Pilgrimage of the Sacred Sites`. Quetes de zone secondaires (6) : `quest_zone_foret_meute=The Hungry Pack`, `quest_zone_foret_venin=Sentinel Against Venom`, `quest_zone_mines_automates=Malfunctioning Automatons`, `quest_zone_marais_prime=Bounty on the Undead`, `quest_zone_marais_appat=Poisoned Bait`, `quest_zone_montagne_aerienne=Aerial Threat`. Quetes de faction intro (4) : `quest_faction_mages_intro=Arcane Echoes`, `quest_faction_chevaliers_intro=The Knight's Oath`, `quest_faction_ombres_intro=In the Shadow of the Goblins`, `quest_faction_marchands_intro=Safe Roads for the Guild`. Quetes de chasse supplementaires (2) : `quest_hunt_scorpions=Scourge of the Sands`, `quest_hunt_gargoyles=The Stone Watchers`. Quetes a choix moral (5) : `quest_moral_contrebandier=The Unmasked Smuggler`, `quest_moral_prisonnier=The Condemned Prisoner`, `quest_moral_grimoire=The Forbidden Grimoire`, `quest_moral_ferme_brulee=The Burned Farm`, `quest_moral_relique=The Relic of the Forgotten Temple`. Quetes de defense (2) : `quest_defend_forest=Defense of the Dark Forest`, `quest_defend_mines=Save the Deep Mines`. Quetes d'escorte (2) : `quest_escort_merchant=Escort the Wandering Merchant`, `quest_escort_refugee=Refugees from the Swamp`. Quete puzzle (1) : `quest_puzzle_sphinx=The Stone Sphinx's Riddle`. La derniere quete `quest_puzzle_ancient_runes` reste a traduire dans une sous-phase ulterieure (limite de 50 lignes ajoutees a un fichier >400 lignes : 48 lignes ici). +48 lignes ajoutees a `QuestFixtures.php` (2054 au total). Aucune migration, aucun template, aucun controller touche.
      - [x] **3e.c.d.quest.j — Fixtures EN pour les dernieres quetes (puzzle runes + quetes d'evenements)** (2026-04-24) : cloture de la couverture EN sur les fixtures de quetes. Finalise `quest_puzzle_ancient_runes=The Ancient Runes` dans `QuestFixtures.php` (derniere quete non traduite mentionnee en 3e.c.d.quest.i). Etend le pattern aux 2 quetes du Festival de la Lune dans `GameEventFixtures.php` : `quest_festival_hunt=Star Hunt`, `quest_festival_collect=Stellar Offering`. La boucle `foreach ($eventQuests as ...)` de `GameEventFixtures::load` est cablee avec le meme pattern que `QuestFixtures` (isset + is_array + setNameTranslations / setDescriptionTranslations) pour supporter les futures traductions d'events. Resultat : 86/86 quetes dans `QuestFixtures` + 2/2 quetes d'evenements = **100% des fixtures de quetes traduites EN**. Aucune migration necessaire (infra posee en 3e.c.d.quest + 3e.c.d.quest.d). Zero impact FR (fallback transparent preserve). Impact EN immediat sur `/game/quests`, `/game` dashboard (via les filters `localized_quest_name` / `localized_quest_description` cables en 3e.c.d.quest.b / 3e.c.d.quest.e) et la quete finale du puzzle runes.
      - [x] **3e.c.domain — Infrastructure multilingue pour les titres de domaines** (2026-04-24) : colonne JSON `title_translations` ajoutee sur `game_domains` (migration `Version20260424DomainTitleTranslations`, idempotente via `ADD COLUMN IF NOT EXISTS` / `DROP COLUMN IF EXISTS`). Entite `Domain` etendue avec `getLocalizedTitle(?string $locale): string` (fallback gracieux sur `$this->title` si locale nulle/vide, colonne nulle, locale absente ou valeur blanche/non-string), `getTitleTranslations(): array` (defaut `[]`) et `setTitleTranslations(?array $translations): self` avec normalisation (cles vides filtrees, valeurs non-string ignorees via `is_string`, valeurs blanches filtrees via `trim`, compaction vers `null` si aucune entree valide). Miroir strict des sous-phases 3a (Item), 3e.a (Monster), 3e.c.d.quest (Quest) et 3e.c.skill (Skill title). Tests `DomainLocalizationTest` (7 cas). Aucun template ni controller modifie : les sous-phases de cablage (filter Twig `localized_domain_title` + templates arbres de talent `/game/skills`) et de fixtures EN suivront. Sous-phase independante des 7 PR i18n en vol : aucune ne touche `Domain.php` ni la table `game_domains`, zero risque de conflit.
      - [x] **3e.c.achievement — Infrastructure multilingue pour les titres et descriptions d'achievements** (2026-04-24) : colonnes JSON `title_translations` et `description_translations` ajoutees sur `game_achievements` (migration `Version20260424AchievementTranslations`, idempotente via `ADD COLUMN IF NOT EXISTS` / `DROP COLUMN IF EXISTS`). Entite `Achievement` etendue avec `getLocalizedTitle(?string $locale): string` et `getLocalizedDescription(?string $locale): string` (fallback gracieux sur `$this->title` / `$this->description` si locale nulle/vide, colonne nulle, locale absente ou valeur blanche/non-string), `getTitleTranslations(): array` + `getDescriptionTranslations(): array` (defaut `[]`) et `setTitleTranslations(?array $translations): self` + `setDescriptionTranslations(?array $translations): self` avec normalisation (cles vides filtrees, valeurs non-string ignorees via `is_string`, valeurs blanches filtrees via `trim`, compaction vers `null` si aucune entree valide). Miroir strict des sous-phases 3a (Item), 3e.a (Monster), 3e.c.d.quest + 3e.c.d.quest.d (Quest) et 3e.c.domain (Domain). Tests `AchievementLocalizationTest` (14 cas : 7 pour `title` + 7 pour `description`). Aucun template (`templates/game/achievements/index.html.twig` ligne 69 pour `title` + ligne 83 pour `description`) ni controller modifie : les sous-phases de cablage (filter Twig `localized_achievement_title` + `localized_achievement_description`) et de fixtures EN (127 achievements) suivront.
      - [x] **3e.c.achievement.b — Cablage des filters `localized_achievement_title` + `localized_achievement_description`** (2026-04-24) : nouvelle extension Twig `App\Twig\AchievementLocalizationExtension` qui expose 2 filters (`localized_achievement_title`, `localized_achievement_description`) appliquant respectivement `Achievement::getLocalizedTitle` et `Achievement::getLocalizedDescription` avec la locale courante recuperee depuis `RequestStack` (fallback transparent sur `Achievement::title` / `Achievement::description` si RequestStack vide, Achievement null ou traduction manquante). Noms distincts de `localized_name` (Item), `localized_monster_name` (Monster), `localized_quest_name` / `localized_quest_description` (Quest) et `localized_domain_title` (Domain) pour eviter la collision Twig entre filters typehintes sur entites differentes. Template cable avec `{{ achievement|localized_achievement_title }}` et `{{ achievement|localized_achievement_description }}` : `templates/game/achievements/index.html.twig` (2 occurrences : en-tete du succes ligne 69, description ligne 83 — les 2 sont deja gardees derriere la branche `isHidden and not isCompleted` donc les succes secrets non decouverts continuent d'afficher `???` / message fixe). Tests `AchievementLocalizationExtensionTest` (9 cas : 4 pour `localized_achievement_title`, 4 pour `localized_achievement_description`, 1 pour l'enregistrement des 2 filters). Une fois la sous-phase 3e.c.achievement.c (fixtures EN pour les 127 achievements) livree, chaque paire `{fr, en}` sera automatiquement affichee dans le template cable sans redeploiement.
      - [x] **3e.c.achievement.c — Fixtures EN pour les 126 achievements** (2026-04-24) : `AchievementFixtures` accepte desormais les cles optionnelles `title_translations` (array) et `description_translations` (array) dans chaque entree du tableau d'achievements, qui sont passees respectivement a `Achievement::setTitleTranslations()` et `Achievement::setDescriptionTranslations()` lors de la construction. Les **126 achievements** existants recoivent une traduction EN complete : combat kills (108 achievements couvrant les 36 monstres du bestiaire x 3 tiers 10/50/100), boss de zone (2 : forest_guardian + forge_lord), exploration (3 : discover-5/10/20), quetes (4 : quest-5/10/25/50), donjons (4), recolte (4), craft (4), trame narrative Acte 3 (1), secrets (7) et tutoriel (1). Titres conservent l'esprit FR : "Nettoyeur de gelees" -> "Slime Cleaner", "Chasseur de gelees" -> "Slime Hunter", "Exterminateur de gelees" -> "Slime Exterminator", etc. Aucune migration necessaire : les colonnes JSON `title_translations` + `description_translations` sont deja creees par la sous-phase 3e.c.achievement. Aucun nouveau test : `AchievementLocalizationTest` couvre deja la mecanique (14 cas : 7 pour title + 7 pour description) et `AchievementLocalizationExtensionTest` (9 cas) couvre le cablage. Zero impact FR (le fallback transparent de `Achievement::getLocalizedTitle` / `getLocalizedDescription` preserve le rendu actuel) ; impact EN immediat sur `/game/achievements` via les filters `localized_achievement_title` / `localized_achievement_description` cables en 3e.c.achievement.b. Atteint **100% de parite FR/EN sur les titres et descriptions des 126 achievements**, fermant la boucle i18n achievements.
      - [x] **3e.c.domain.c — Fixtures EN pour les titres de domaines (32 domaines)** (2026-04-24) : `DomainFixtures` accepte desormais la cle optionnelle `title_translations` (array) dans chaque entree du tableau de domaines, qui est passee a `Domain::setTitleTranslations()` lors de la construction (juste apres `setElement`). Les 32 domaines existants recoivent une traduction EN : combat feu (3) — pyromancy=Pyromancer, berserker=Berserker, artificer=Artificer ; combat eau (3) — hydromancer=Hydromancer, healer=Healer, tidecaller=Tidecaller ; combat air (3) — stormcaller=Stormcaller, archer=Archer, wanderer=Wanderer ; combat terre (3) — geomancer=Geomancer, defender=Defender, guardian=Guardian ; combat metal (3) — soldier=Soldier, knight=Knight, engineer=Engineer ; combat bete (3) — hunter=Hunter, tamer=Tamer, druid=Druid ; combat lumiere (3) — paladin=Paladin, priest=Priest, inquisitor=Inquisitor ; combat ombre (3) — assassin=Assassin, necromancer=Necromancer, warlock=Warlock ; recolte (4) — miner=Miner, herbalist=Herbalist, fisherman=Fisherman, skinner=Skinner ; craft (4) — blacksmith=Blacksmith, leatherworker=Leatherworker, alchimist=Alchemist, jeweller=Jeweler. Aucune migration necessaire : la colonne JSON `title_translations` est deja creee par la sous-phase 3e.c.domain. Aucun nouveau test : `DomainLocalizationTest` couvre deja la mecanique (7 cas). Zero impact FR (le fallback transparent de `Domain::getLocalizedTitle` preserve le rendu actuel) ; impact EN immediat sur `/game/skills` (chip horizontal mobile, sidebar groupee par element, sidebar "Autres", en-tete h2 du panneau actif) et `domain_info.html.twig` (block title + en-tete h1) via le filter `localized_domain_title` cable en 3e.c.domain.b. Atteint **100% de parite FR/EN sur les titres des 32 arbres de talent**. Le projet affiche desormais l'integralite des noms d'items starter (35 items + 26 materia + 18 materiaux craft + 27 plantes + 15 armes generiques), des noms de monstres (tout le bestiaire), des noms + descriptions de quetes (88/88), et des titres de domaines (32/32) en EN.
      - [x] **3e.c.statuseffect — Infrastructure multilingue + cablage + fixtures EN pour les noms d'effets de statut** (2026-04-25) : colonne JSON `name_translations` ajoutee sur `game_status_effects` (migration `Version20260425StatusEffectNameTranslations`, idempotente via `ADD COLUMN IF NOT EXISTS` / `DROP COLUMN IF EXISTS`). Entite `StatusEffect` etendue avec `getLocalizedName(?string $locale): string` (fallback gracieux sur `$this->name` si locale nulle/vide, colonne nulle, locale absente ou valeur blanche/non-string), `getNameTranslations(): array` (defaut `[]`) et `setNameTranslations(?array $translations): self` avec normalisation (cles vides filtrees, valeurs non-string ignorees via `is_string`, valeurs blanches filtrees via `trim`, compaction vers `null` si aucune entree valide). Miroir strict des sous-phases 3a (Item), 3e.a (Monster), 3e.c.d.quest (Quest) et 3e.c.domain (Domain). Nouveau filter Twig `localized_status_effect_name` (extension `App\Twig\StatusEffectLocalizationExtension`) qui applique `StatusEffect::getLocalizedName` avec la locale courante recuperee depuis `RequestStack` (fallback transparent). Templates cables : `templates/game/fight/index.html.twig` (2 occurrences : tooltip + libelle de l'icone) et `templates/game/fight/partials/_timeline.html.twig` (1 occurrence : tooltip de la timeline). Fixtures EN dans `StatusEffectFixtures` cables sur les **15 effets de statut** existants : Poison, Virulent Poison, Burn, Insidious Poison, Paralysis, Freeze, Silence, Regeneration, Greater Regeneration, Shield, Arcane Shield, Berserk, Hearty Meal, Strength Elixir, Natural Regeneration. Tests : `StatusEffectLocalizationTest` (7 cas) + `StatusEffectLocalizationExtensionTest` (5 cas). Zero impact FR (le fallback transparent preserve le rendu actuel) ; impact EN immediat sur l'ecran de combat (badges des effets actifs + tooltips de la timeline). Atteint **100% de parite FR/EN sur les 15 effets de statut**.
      - [x] **3e.c.domain.b — Cablage du filter `localized_domain_title`** (2026-04-24) : nouveau filter Twig `localized_domain_title` (extension `App\Twig\DomainLocalizationExtension`) qui applique `Domain::getLocalizedTitle` avec la locale courante recuperee depuis `RequestStack` (fallback transparent sur `Domain::title` si RequestStack vide, Domain null ou traduction manquante). Signature `Domain|DomainModel|null` pour accepter a la fois l'entite brute et le DTO utilise par les templates : `DomainModel` expose desormais un `public readonly Domain $entity` permettant au filter de deleguer a l'entite sans dupliquer les traductions dans le DTO. Nom distinct de `localized_name` (Item), `localized_monster_name` (Monster) et `localized_quest_name` (Quest) pour eviter la collision Twig entre filters typehintes sur entites differentes. Templates cables avec `{{ domain|localized_domain_title }}` : `templates/game/skills/domain_info.html.twig` (2 occurrences : block title + en-tete h1) et `templates/game/skills/index.html.twig` (4 occurrences : chip horizontal mobile, sidebar groupee par element, sidebar domaines "Autres", en-tete h2 du panneau actif). Les templates `templates/admin/**`, `templates/game/index.html.twig` (dashboard recap) et `templates/game/profile/show.html.twig` (experience par domaine) ne sont pas cables (hors scope sous-phase). Tests `DomainLocalizationExtensionTest` (6 cas : traduction matchee, acceptation d'un DomainModel, fallback base title, RequestStack vide, Domain null, enregistrement du filter). Une fois la sous-phase 3e.c.domain.c (fixtures EN pour les titres de domaines) livree, chaque paire `{fr, en}` sera automatiquement affichee dans les 2 templates cables sans redeploiement de template.
      - [x] **3e.h — Infrastructure + cablage + fixtures EN pour les noms et descriptions de factions** (2026-04-26) : colonnes JSON `name_translations` et `description_translations` ajoutees sur `game_factions` (migration `Version20260426FactionTranslations`, idempotente via `ADD COLUMN IF NOT EXISTS` / `DROP COLUMN IF EXISTS`). Entite `Faction` etendue avec `getLocalizedName(?string $locale): string` et `getLocalizedDescription(?string $locale): string` (fallback gracieux sur `$this->name` / `$this->description` si locale nulle/vide, colonne nulle, locale absente ou valeur blanche/non-string), `getNameTranslations` + `setNameTranslations`, `getDescriptionTranslations` + `setDescriptionTranslations` avec normalisation identique au pattern Race (3e.f) / Region (3e.g) (cles vides filtrees, valeurs non-string ignorees via `is_string`, valeurs blanches filtrees via `trim`, compaction vers `null`). Tests `FactionLocalizationTest` (15 cas : 7 pour `name`, 7 pour `description` + 1 cas additionnel `getDescriptionTranslationsDefaultsToEmptyArray`). Nouvelle extension Twig `App\Twig\FactionLocalizationExtension` exposant 2 filters (`localized_faction_name`, `localized_faction_description`) qui appellent `RequestStack::getCurrentRequest()->getLocale()` puis delegue a l'entite (fallback transparent sur RequestStack vide / Faction null / traduction manquante). Tests `FactionLocalizationExtensionTest` (9 cas). Template cable : `templates/game/factions/index.html.twig` (2 occurrences : `faction.name` -> `faction|localized_faction_name`, `faction.description` -> `faction|localized_faction_description`). `FactionFixtures` recoit les traductions EN sur les 4 factions standards via les nouvelles cles optionnelles `name_translations` + `description_translations` dans le tableau de fixtures, passees a `setNameTranslations` / `setDescriptionTranslations` apres les setters FR : `Guilde des Marchands=Merchants Guild`, `Ordre des Chevaliers=Order of Knights`, `Cercle des Mages=Circle of Mages`, `Confrérie des Ombres=Brotherhood of Shadows` cote nom + descriptions complementaires (routes commerciales / ordre militaire / arcanes / reseau clandestin). Atteint **100% de parite FR/EN sur les 4 factions du jeu** en une seule sous-phase (infra + cablage + fixtures), pattern eprouve par 3e.f Race et 3e.g Region. Sous-phase independante des 13+ PR i18n / avatar / mounts / events en vol : aucune ne touche `Faction.php` ni la table `game_factions`, zero risque de conflit.
      - [x] **3e.o — Infrastructure + cablage API + fixtures EN pour les noms et descriptions de festivals** (2026-04-28) : colonnes JSON `name_translations` et `description_translations` ajoutees sur `festival` (migration `Version20260428FestivalTranslations`, idempotente via `ADD COLUMN IF NOT EXISTS` / `DROP COLUMN IF EXISTS`). Entite `Festival` etendue avec `getLocalizedName(?string $locale): string` et `getLocalizedDescription(?string $locale): ?string` (signature avec retour nullable car `Festival::description` est lui-meme nullable, comme Recipe en 3e.j, FactionReward en 3e.h.b et EnchantmentDefinition en 3e.m), `getNameTranslations` + `setNameTranslations`, `getDescriptionTranslations` + `setDescriptionTranslations` avec normalisation identique au pattern Race / Region / Faction / Dungeon / Recipe / Mount / Enchantment / Map (cles vides filtrees, valeurs non-string ignorees via `is_string`, valeurs blanches filtrees via `trim`, compaction vers `null`). Tests `FestivalLocalizationTest` (16 cas : 7 pour `name`, 9 pour `description` couvrant explicitement le retour `null` quand description et translations sont nulles + `getDescriptionTranslationsDefaultsToEmptyArray`). Cablage cote API uniquement : `GameTimeController::__invoke()` injecte desormais `Symfony\Component\HttpFoundation\Request`, lit `$request->getLocale()` et appelle `getLocalizedName($locale)` / `getLocalizedDescription($locale)` pour chaque festival actif. Aucun template Twig modifie : Festival est expose uniquement via l'API `/api/game/time` consommee par le HUD PixiJS (`map_pixi_controller.js` ligne 1781 affiche `✦ ${festival.name}`), donc la traduction est realisee cote serveur avant la serialisation JSON. `FestivalFixtures` recoit les traductions EN sur les 4 festivals saisonniers via les nouvelles cles optionnelles `name_translations` + `description_translations` du tableau, propagees a `setNameTranslations` / `setDescriptionTranslations` apres les setters FR : `Fete du Renouveau=Renewal Festival`, `Solstice de Flamme=Flame Solstice`, `Moisson des Ames=Soul Harvest`, `Nuit Eternelle=Eternal Night` cote nom + descriptions complementaires (spring awakens nature / sun stands at zenith / veils between worlds grow thin / biting cold blankets the world). Atteint **100% de parite FR/EN sur les 4 festivals saisonniers du jeu** en une seule sous-phase (infra + cablage + fixtures + tests), pattern eprouve par 3e.f / 3e.g / 3e.h / 3e.i / 3e.j / 3e.l / 3e.m / 3e.n. Sous-phase independante des 15 PR i18n / avatar / mounts / events en vol : aucune ne touche `Festival.php`, `FestivalFixtures.php` ni la table `festival`, zero risque de conflit.
      - [x] **3e.h.b — Infrastructure + cablage + fixtures EN pour les libelles et descriptions des recompenses de faction** (2026-04-28) : colonnes JSON `label_translations` et `description_translations` ajoutees sur `game_faction_rewards` (migration `Version20260428FactionRewardTranslations`, idempotente via `ADD COLUMN IF NOT EXISTS` / `DROP COLUMN IF EXISTS`). Entite `FactionReward` etendue avec `getLocalizedLabel(?string $locale): string` et `getLocalizedDescription(?string $locale): ?string` (signature avec retour nullable car `FactionReward::description` est lui-meme nullable, comme Recipe en 3e.j et EnchantmentDefinition en 3e.m), `getLabelTranslations` + `setLabelTranslations`, `getDescriptionTranslations` + `setDescriptionTranslations` avec normalisation identique au pattern Race / Region / Faction / Dungeon / Recipe / Mount / Enchantment (cles vides filtrees, valeurs non-string ignorees via `is_string`, valeurs blanches filtrees via `trim`, compaction vers `null`). Tests `FactionRewardLocalizationTest` (15 cas : 7 pour `label`, 8 pour `description` couvrant explicitement le retour `null` quand description et translations sont nulles). Nouvelle extension Twig `App\Twig\FactionRewardLocalizationExtension` exposant 2 filters (`localized_faction_reward_label`, `localized_faction_reward_description`) qui appellent `RequestStack::getCurrentRequest()->getLocale()` puis delegue a l'entite. Le filter description retourne explicitement `''` quand la description est null (compatibilite Twig avec `{{ ... }}`). Tests `FactionRewardLocalizationExtensionTest` (10 cas). Template cable : `templates/game/factions/index.html.twig` (1 occurrence : `reward.label` -> `reward|localized_faction_reward_label` et `reward.description` -> `reward|localized_faction_reward_description` dans le tooltip). `FactionRewardFixtures` recoit les traductions EN sur les 12 recompenses standards via les nouvelles cles `label_en` et `description_en` du tableau `$rewards`, propagees a `setLabelTranslations` / `setDescriptionTranslations` apres les setters FR : 3 recompenses Marchands (Merchant Discount / Honorary Discount / Privileged Pricing), 3 Chevaliers (Knight's Blessing / Shield of the Order / Champion of the Order), 3 Mages (Arcane Lore / Magical Resonance / Honorary Archmage), 3 Ombres (Thief's Instinct / Shadow Step / Master Assassin) cote label + descriptions complementaires. Atteint **100% de parite FR/EN sur les 12 recompenses de faction du jeu** en une seule sous-phase (infra + cablage + fixtures + tests), pattern eprouve par 3e.h Faction. Sous-phase independante des 15 PR i18n / avatar / mounts / events en vol : aucune ne touche `FactionReward.php` ni la table `game_faction_rewards`, zero risque de conflit.
      - [x] **3e.n — Infrastructure + cablage Twig + fixtures EN pour les noms des cartes** (2026-04-28) : colonne JSON `name_translations` ajoutee sur `map` (migration `Version20260428MapNameTranslations`, idempotente via `ADD COLUMN IF NOT EXISTS` / `DROP COLUMN IF EXISTS`). Entite `Map` etendue avec `getLocalizedName(?string $locale): string` (fallback gracieux sur `$this->name` si locale nulle/vide, colonne nulle, locale absente ou valeur blanche/non-string), `getNameTranslations(): array` (defaut `[]`) et `setNameTranslations(?array $translations): self` avec normalisation (cles vides filtrees, valeurs non-string ignorees via `is_string`, valeurs blanches filtrees via `trim`, compaction vers `null`). Tests `MapLocalizationTest` (7 cas : fallback no-translation, match locale EN/DE, fallback locale missing, ignore blank/invalid, reset null, reset only-invalid, default empty array). Nouvelle extension Twig `App\Twig\MapLocalizationExtension` exposant 1 filter `localized_map_name` qui applique `Map::getLocalizedName` avec la locale courante recuperee depuis `RequestStack` (fallback transparent sur `Map::name` si RequestStack vide, Map null ou traduction manquante). Tests `MapLocalizationExtensionTest` (5 cas). Templates cables : `templates/game/index.html.twig` (2 occurrences : dashboard hint + statistique current_map), `templates/game/quest/index.html.twig` (2 occurrences : carte du donneur de quete dans le journal et l'historique), `templates/game/chat/index.html.twig` (1 occurrence : onglet chat carte), `templates/game/character/select.html.twig` (1 occurrence : libelle de carte sous le personnage). Aucun template `templates/admin/**` cable (hors scope jeu, convention identique aux precedentes sous-phases). `MapFixtures` recoit les traductions EN sur les 8 cartes existantes via `setNameTranslations(['en' => ...])` apres `setName` : `Carte de test=Test Map`, `Village de Lumiere=Village of Light`, `Foret des murmures=Whispering Forest`, `Mines profondes=Deep Mines`, `Marais Brumeux=Misty Swamp`, `Crete de Ventombre=Shadowind Ridge`, `Racines de la foret (donjon)=Roots of the Forest (dungeon)`, `Nexus de la Convergence (donjon)=Nexus of Convergence (dungeon)`. Atteint **100% de parite FR/EN sur les 8 cartes du jeu** en une seule sous-phase (infra + cablage + fixtures + tests), pattern eprouve par 3e.f Race / 3e.g Region / 3e.h Faction / 3e.i Dungeon / 3e.l Mount / 3e.m Enchantment. Sous-phase independante des 15 PR i18n / avatar / mounts / events en vol : aucune ne touche `Map.php`, `MapFixtures.php` ni la table `map`, zero risque de conflit.
      - [x] **3e.j.e — Fixtures EN pour les noms de recettes (joaillerie 16 + 4 masterworks = 20 recettes — 100% des recettes traduites)** (2026-04-27) : cloture la couverture EN sur les fixtures de recettes apres 3e.j.b (forgeron 19), 3e.j.c (tannerie 15) et 3e.j.d (alchimie 11). Couvre les 16 dernieres recettes du metier `joaillier` + les 4 chefs-d'oeuvre exclusifs aux maitres artisans (task 122). Joaillerie T0 (1) : `Anneau de cuivre=Copper Ring`. Joaillerie T1 bases (4) : `Taille de gemme brute=Cut Gem`, `Anneau de fer=Iron Ring`, `Amulette de fer=Iron Amulet`, `Bracelet de fer=Iron Bracelet`. Joaillerie T2 or et gemmes fines (4) : `Taille de gemme fine=Fine Gem`, `Anneau d'or serti=Jeweled Gold Ring`, `Amulette d'or=Gold Amulet`, `Couronne d'or=Gold Crown`. Joaillerie T3 mithril et gemmes rares (4) : `Taille de gemme rare=Rare Gem`, `Enchantement de gemme=Enchanted Gem`, `Anneau de mithril serti=Jeweled Mithril Ring`, `Amulette de mithril=Mithril Amulet`. Joaillerie T4 prismatique (1) : `Gemme prismatique=Prismatic Gem`. Joaillerie T5 legendaires (2) : `Anneau prismatique=Prismatic Ring`, `Amulette prismatique=Prismatic Amulet`. Masterworks (4, 1 par metier) : `Lame du maitre forgeron=Master Blacksmith's Blade`, `Manteau du maitre tanneur=Master Leatherworker's Cloak`, `Grand elixir du maitre alchimiste=Master Alchemist's Grand Elixir`, `Anneau du maitre joaillier=Master Jeweler's Ring`. Les noms EN reprennent strictement ceux des items produits (3c.s/t/u) pour assurer la coherence entre la carte de craft et l'item recu en inventaire. +20 lignes ajoutees a `RecipeFixtures.php` (1088 au total, dans la limite de 50 lignes ajoutees a un fichier > 400 lignes). Aucune migration, aucun nouveau test : infrastructure (3e.j) + cablage Twig (3e.j) deja en place, `RecipeLocalizationTest` (16 cas) + `RecipeLocalizationExtensionTest` (9 cas) couvrent deja la mecanique. Resultat : **65/65 recettes traduites = 100% de parite FR/EN sur l'integralite du systeme de craft** (forgeron 19 + tannerie 15 + alchimie 11 + joaillerie 16 + masterworks 4). Impact EN immediat sur `templates/game/crafting/_recipe_card.html.twig` + `_recipe_card_locked.html.twig` pour toutes les recettes du jeu via le filter `localized_recipe_name` cable en 3e.j. Sous-phase independante des 14 PR i18n / avatar / mounts / events en vol : aucune ne touche `RecipeFixtures.php`.
      - [x] **3e.j.d — Fixtures EN pour les noms de recettes (section alchimie : 11 recettes)** (2026-04-27) : suite directe des sous-phases 3e.j.b (forgeron) et 3e.j.c (tannerie). Couvre les 11 recettes standard du metier `alchimiste` via la convention `name_translations` deja cablee dans le loader (le 12e alchimiste `Grand elixir du maitre alchimiste` est un masterwork couvert dans une sous-phase ulterieure dediee). 11 recettes traduites : Potions de soin (3) `Minor Healing Potion / Healing Potion / Major Healing Potion`, Antidote (1) `Antidote`, Alchimie de base (3) `Potion Base / Healing Salve / Minor Energy Potion`, Concentre toxique (1) `Poison Vial`, Elixirs (3) `Strength Elixir / Defense Elixir / Vitality Elixir`. Diff : 11 lignes ajoutees a `RecipeFixtures.php` (1068 lignes total, dans la limite de 50 lignes ajoutees a un fichier > 400 lignes). Aucune migration, aucun nouveau test (`RecipeLocalizationTest` + `RecipeLocalizationExtensionTest` couvrent deja la mecanique). Impact EN immediat sur `templates/game/crafting/_recipe_card.html.twig` + `_recipe_card_locked.html.twig` pour les 11 recettes alchimiste. 20 recettes restent sans traduction (joaillerie 18 + 3 masterworks restants) — couvertes en sous-phases ulterieures.
      - [x] **3e.j.c — Fixtures EN pour les noms de recettes (section tannerie : 15 recettes)** (2026-04-27) : suite directe de la sous-phase 3e.j.b qui couvre l'integralite du metier `tanneur` via la meme convention `name_translations` (loader deja cable en 3e.j.b). 15 recettes traduites : Tannerie starter (3) `Leather Boots / Leather Hat / Leather Armor`, Tannerie T1 accessoires (5) `Leather Strip / Leather Gloves / Leather Belt / Leather Pauldrons / Leather Greaves`, Tannerie T2 cuir renforce (6) `Hardened Leather Vest / Hardened Leather Boots / Hardened Leather Gloves / Hardened Leather Belt / Hardened Leather Pauldrons / Hardened Leather Greaves`, Tannerie T3 cuir exotique (1) `Exotic Leather Vest`. Diff : 15 lignes ajoutees a `RecipeFixtures.php` (1057 lignes total, dans la limite de 50 lignes ajoutees a un fichier > 400 lignes). Aucune migration, aucun nouveau test (`RecipeLocalizationTest` + `RecipeLocalizationExtensionTest` couvrent deja la mecanique). Impact EN immediat sur `templates/game/crafting/_recipe_card.html.twig` + `_recipe_card_locked.html.twig` pour les 15 recettes tannerie. 31 recettes restent sans traduction (alchimie, joaillerie, masterworks) — couvertes en sous-phases ulterieures.
      - [x] **3e.j.b — Fixtures EN pour les noms de recettes (section forgeron : 19 recettes)** (2026-04-27) : suite directe de la sous-phase 3e.j (infrastructure livree le meme jour). Le loader `RecipeFixtures::load` lit desormais une cle optionnelle `name_translations` du tableau de fixtures et appelle `Recipe::setNameTranslations` apres les setters FR (pattern miroir de `MonsterFixtures`, `ItemFixtures`, `QuestFixtures`). 19 recettes du metier `forgeron` recoivent leur traduction EN via `'name_translations' => ['en' => '...']` : Forge T1 (4) `Iron Dagger / Short Sword / Iron Shield / Iron Helmet`, Lingots (5) `Bronze Ingot / Cobalt Ingot / Mithril Ingot / Adamantite Ingot / Orichalcum Ingot`, Forge T2 armures fer (4) `Iron Chestplate / Iron Greaves / Iron Boots / Iron Gauntlets`, Forge T3 armures mithril (6) `Mithril Helm / Mithril Cuirass / Mithril Greaves / Mithril Sabatons / Mithril Gauntlets / Mithril Pauldrons`. Aucune migration, aucun nouveau test (l'infrastructure et les filters Twig de 3e.j sont deja couverts par `RecipeLocalizationTest` + `RecipeLocalizationExtensionTest`). Diff : 23 lignes ajoutees a `RecipeFixtures.php` (1042 lignes total, dans la limite de 50 lignes ajoutees a un fichier > 400 lignes). Impact EN immediat sur `templates/game/crafting/_recipe_card.html.twig` + `_recipe_card_locked.html.twig` (les 2 templates cables en 3e.j) pour les 19 recettes forgeron quand l'utilisateur passe sa locale en EN via `/game/settings`. 46 recettes restent sans traduction (tannerie, alchimie, joaillerie, masterworks) — couvertes en sous-phases ulterieures dediees pour rester sous le budget de modifications par sous-phase. Sous-phase independante des 14 PR ouvertes : aucune ne touche `RecipeFixtures.php`, zero risque de conflit.
      - [x] **3e.j — Infrastructure + cablage Twig pour les noms et descriptions de recettes** (2026-04-27) : colonnes JSON `name_translations` et `description_translations` ajoutees sur `game_recipes` (migration `Version20260427RecipeTranslations`, idempotente via `ADD COLUMN IF NOT EXISTS` / `DROP COLUMN IF EXISTS`). Entite `Recipe` etendue avec `getLocalizedName(?string $locale): string` et `getLocalizedDescription(?string $locale): ?string` (signature avec retour nullable car `Recipe::description` est lui-meme nullable, contrairement aux autres entites de ce pattern), `getNameTranslations` + `setNameTranslations`, `getDescriptionTranslations` + `setDescriptionTranslations` avec normalisation identique au pattern Race (3e.f) / Region (3e.g) / Faction (3e.h) / Dungeon (3e.i) (cles vides filtrees, valeurs non-string ignorees via `is_string`, valeurs blanches filtrees via `trim`, compaction vers `null`). Tests `RecipeLocalizationTest` (16 cas : 7 pour `name`, 8 pour `description` + 1 cas additionnel `getDescriptionTranslationsDefaultsToEmptyArray`, le 8e couvrant le retour `null` quand description et translations sont nulles). Nouvelle extension Twig `App\Twig\RecipeLocalizationExtension` exposant 2 filters (`localized_recipe_name`, `localized_recipe_description`) qui appellent `RequestStack::getCurrentRequest()->getLocale()` puis delegue a l'entite. Le filter description retourne explicitement `''` quand la description est null (compatibilite Twig avec `{{ ... }}`). Tests `RecipeLocalizationExtensionTest` (9 cas). Templates cables : `templates/game/crafting/_recipe_card.html.twig` (2 occurrences : `recipe.name` -> `recipe|localized_recipe_name`, `recipe.description` -> `recipe|localized_recipe_description`) et `templates/game/crafting/_recipe_card_locked.html.twig` (2 occurrences identiques). Fixtures EN non incluses dans cette sous-phase : `RecipeFixtures.php` fait deja 1019 lignes et 65 recettes a traduire, ce qui depasse la limite de 50 lignes de modifications sur un fichier > 400 lignes (regle 0.1 de l'enonce). Les traductions EN seront livrees en sous-phase dediee 3e.j.b. Sous-phase independante des 14 PR i18n / avatar / mounts / events en vol : aucune ne touche `Recipe.php` ni la table `game_recipes`, zero risque de conflit.
      - [x] **3e.i — Infrastructure + cablage + fixtures EN pour les noms et descriptions de donjons** (2026-04-27) : colonnes JSON `name_translations` et `description_translations` ajoutees sur `game_dungeons` (migration `Version20260427DungeonTranslations`, idempotente via `ADD COLUMN IF NOT EXISTS` / `DROP COLUMN IF EXISTS`). Entite `Dungeon` etendue avec `getLocalizedName(?string $locale): string` et `getLocalizedDescription(?string $locale): string` (fallback gracieux sur `$this->name` / `$this->description` si locale nulle/vide, colonne nulle, locale absente ou valeur blanche/non-string), `getNameTranslations` + `setNameTranslations`, `getDescriptionTranslations` + `setDescriptionTranslations` avec normalisation identique au pattern Race (3e.f) / Region (3e.g) / Faction (3e.h) (cles vides filtrees, valeurs non-string ignorees via `is_string`, valeurs blanches filtrees via `trim`, compaction vers `null`). Tests `DungeonLocalizationTest` (15 cas : 7 pour `name`, 7 pour `description` + 1 cas additionnel `getDescriptionTranslationsDefaultsToEmptyArray`). Nouvelle extension Twig `App\Twig\DungeonLocalizationExtension` exposant 2 filters (`localized_dungeon_name`, `localized_dungeon_description`) qui appellent `RequestStack::getCurrentRequest()->getLocale()` puis delegue a l'entite (fallback transparent sur RequestStack vide / Dungeon null / traduction manquante). Tests `DungeonLocalizationExtensionTest` (9 cas). Templates cables : `templates/game/dungeon/show.html.twig` (4 occurrences : block title, en-tete h2 + description, banniere "Vous etes deja dans le donjon X") et `templates/game/dungeon/list.html.twig` (3 occurrences : banniere "Donjon en cours", carte de donjon h3 + description, ligne d'historique). `DungeonFixtures` recoit les traductions EN sur les 2 donjons existants via `setNameTranslations(['en' => ...])` et `setDescriptionTranslations(['en' => ...])` apres les setters FR : `Racines de la foret=Roots of the Forest`, `Le Nexus de la Convergence=The Nexus of Convergence` cote nom + descriptions complementaires (galleries souterraines corrompues / sanctuaire du cristal d'Amethyste). Atteint **100% de parite FR/EN sur les 2 donjons du jeu** en une seule sous-phase (infra + cablage + fixtures), pattern eprouve par 3e.f Race / 3e.g Region / 3e.h Faction. Sous-phase independante des 14 PR i18n / avatar / mounts / events en vol : aucune ne touche `Dungeon.php` ni la table `game_dungeons`, zero risque de conflit.
      - [x] **3e.m — Infrastructure + cablage Twig + fixtures EN pour les noms et descriptions des enchantements** (2026-04-27) : colonnes JSON `name_translations` et `description_translations` ajoutees sur `game_enchantment_definitions` (migration `Version20260427EnchantmentTranslations`, idempotente via `ADD COLUMN IF NOT EXISTS` / `DROP COLUMN IF EXISTS`). Entite `EnchantmentDefinition` etendue avec `getLocalizedName(?string $locale): string` et `getLocalizedDescription(?string $locale): ?string` (signature avec retour nullable car `EnchantmentDefinition::description` est lui-meme nullable, comme Recipe en 3e.j), `getNameTranslations` + `setNameTranslations`, `getDescriptionTranslations` + `setDescriptionTranslations` avec normalisation identique au pattern Race / Region / Faction / Dungeon / Recipe / Mount (cles vides filtrees, valeurs non-string ignorees via `is_string`, valeurs blanches filtrees via `trim`, compaction vers `null`). Tests `EnchantmentLocalizationTest` (13 cas : 6 pour `name`, 7 pour `description` couvrant explicitement le retour `null` quand description et translations sont nulles). Nouvelle extension Twig `App\Twig\EnchantmentLocalizationExtension` exposant 2 filters (`localized_enchantment_name`, `localized_enchantment_description`) qui appellent `RequestStack::getCurrentRequest()->getLocale()` puis delegue a l'entite. Le filter description retourne explicitement `''` quand la description est null (compatibilite Twig avec `{{ ... }}`). Tests `EnchantmentLocalizationExtensionTest` (7 cas). Template cable : `templates/game/crafting/_enchantment.html.twig` (3 occurrences : enchantements actifs en haut, options du `<select>` du formulaire, bloc details ingredients). `EnchantmentDefinitionFixtures::getDefinitionsData` accepte desormais 2 cles optionnelles `name_translations` et `description_translations` propagees via `setNameTranslations` / `setDescriptionTranslations` sur l'entite. Les 4 enchantements existants recoivent leur traduction EN : `Tranchant de feu=Flame Edge`, `Protection de glace=Ice Ward`, `Robustesse tellurique=Telluric Fortitude`, `Precision lumineuse=Radiant Precision` cote nom + descriptions complementaires (searing flame / protective veil of frost / strength of the earth / guides each strike with light). Atteint **100% de parite FR/EN sur les 4 enchantements du jeu** en une seule sous-phase (infra + cablage + fixtures + tests), pattern eprouve par 3e.f / 3e.g / 3e.h / 3e.i / 3e.j / 3e.l. Sous-phase independante des 15 PR i18n / avatar / mounts / events en vol : aucune ne touche `EnchantmentDefinition.php` ni la table `game_enchantment_definitions`, zero risque de conflit.
      - [x] **3e.l — Infrastructure + cablage Twig + fixtures EN pour les noms et descriptions de montures** (2026-04-27) : colonnes JSON `name_translations` et `description_translations` ajoutees sur `game_mounts` (migration `Version20260427MountTranslations`, idempotente via `ADD COLUMN IF NOT EXISTS` / `DROP COLUMN IF EXISTS`). Entite `Mount` etendue avec `getLocalizedName(?string $locale): string` et `getLocalizedDescription(?string $locale): string` (fallback gracieux sur `$this->name` / `$this->description` si locale nulle/vide, colonne nulle, locale absente ou valeur blanche/non-string), `getNameTranslations` + `setNameTranslations`, `getDescriptionTranslations` + `setDescriptionTranslations` avec normalisation identique au pattern Race (3e.f) / Region (3e.g) / Faction (3e.h) / Dungeon (3e.i) / EquipmentSet (3e.k). Tests `MountLocalizationTest` (15 cas : 7 pour `name`, 7 pour `description` + 1 cas additionnel `getDescriptionTranslationsDefaultsToEmptyArray`). Nouvelle extension Twig `App\Twig\MountLocalizationExtension` exposant 2 filters (`localized_mount_name`, `localized_mount_description`) qui appellent `RequestStack::getCurrentRequest()->getLocale()` puis delegue a l'entite. Tests `MountLocalizationExtensionTest` (9 cas). Aucun template cable : aucune vue n'expose actuellement `Mount` (la sous-phase 130 s2a en cours via PR #428 introduit `PlayerMount` mais pas d'UI). Les filters seront prets quand les sous-phases 130 s3 (vitesse +50%) et 130 s4 (animation sprite) introduiront les ecrans de selection / preview de monture. `MountFixtures` recoit les traductions EN sur les 4 montures existantes via `setNameTranslations(['en' => ...])` et `setDescriptionTranslations(['en' => ...])` apres les setters FR : `Cheval brun=Brown Horse`, `Loup sauvage=Dire Wolf`, `Chocobo jaune=Yellow Chocobo`, `Sanglier colossal=Colossal Boar` cote nom + descriptions complementaires (compagnon endurant / loup apprivoise apres une longue chaine de quetes / oiseau geant legendaire / sanglier massif drop boss). Atteint **100% de parite FR/EN sur les 4 montures du jeu** en une seule sous-phase (infra + cablage + fixtures), pattern eprouve par 3e.f / 3e.g / 3e.h / 3e.i / 3e.k. Sous-phase independante des 15 PR i18n / avatar / mounts / events en vol : aucune ne touche `Mount.php` ni la table `game_mounts`, zero risque de conflit (PR #428 n'ajoute que `PlayerMount` et `MountAcquisitionService`).

---

### Definition of Done

- [ ] Load test passe avec 200 joueurs simultanes
- [ ] Traduction anglaise complete de l'interface
- [ ] Selecteur de langue fonctionnel
