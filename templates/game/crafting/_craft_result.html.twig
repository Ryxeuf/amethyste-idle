{# Craft result: trigger toast notifications via JS #}
{% for type, messages in app.flashes %}
    {% for message in messages %}
        <script>
            if (window.Toast) {
                window.Toast.show('{{ type }}', {{ message|json_encode|raw }});
            }
        </script>
    {% endfor %}
{% endfor %}
